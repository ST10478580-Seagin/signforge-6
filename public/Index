<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width,initial-scale=1"/>
<title>SignForge – AI Document Signing</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,600;0,700;1,600&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet"/>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{font-family:'Outfit',sans-serif;background:#07090f;color:#e2e8f8;line-height:1.65;min-height:100vh;overflow-x:hidden;-webkit-font-smoothing:antialiased}
body::before{content:'';position:fixed;inset:0;z-index:0;pointer-events:none;background:radial-gradient(ellipse 70% 50% at 15% 10%,rgba(99,91,255,.18) 0%,transparent 60%),radial-gradient(ellipse 50% 40% at 85% 85%,rgba(6,214,160,.13) 0%,transparent 55%)}
body::after{content:'';position:fixed;inset:0;z-index:0;pointer-events:none;background-image:linear-gradient(rgba(99,91,255,.025) 1px,transparent 1px),linear-gradient(90deg,rgba(99,91,255,.025) 1px,transparent 1px);background-size:56px 56px}
.page{display:none;position:relative;z-index:1;min-height:100vh;animation:fadeUp .25s ease}
.page.active{display:block}
@keyframes fadeUp{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}
.wrap{max-width:1160px;margin:0 auto;padding:0 32px}
.hidden{display:none!important}

/* ── TYPOGRAPHY ── */
h1,h2,h3{font-family:'Cormorant Garamond',serif;line-height:1.1}
.grad{background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}

/* ── BUTTONS ── */
.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;padding:11px 24px;border-radius:10px;font-family:'Outfit',sans-serif;font-size:.9rem;font-weight:600;cursor:pointer;border:none;text-decoration:none;transition:all .2s;letter-spacing:.01em;white-space:nowrap}
.btn:disabled{opacity:.4;cursor:not-allowed;transform:none!important}
.btn-primary{background:linear-gradient(135deg,#635bff,#4f46e5);color:#fff;box-shadow:0 4px 24px rgba(99,91,255,.4)}
.btn-primary:not(:disabled):hover{transform:translateY(-2px);box-shadow:0 8px 36px rgba(99,91,255,.55)}
.btn-emerald{background:linear-gradient(135deg,#06d6a0,#059669);color:#fff;box-shadow:0 4px 20px rgba(6,214,160,.35)}
.btn-emerald:not(:disabled):hover{transform:translateY(-2px);box-shadow:0 8px 32px rgba(6,214,160,.5)}
.btn-ghost{background:rgba(255,255,255,.06);color:#e2e8f8;border:1px solid rgba(255,255,255,.12)}
.btn-ghost:hover{background:rgba(255,255,255,.1);border-color:rgba(255,255,255,.2)}
.btn-danger{background:#ff4f7b;color:#fff}
.btn-danger:hover{background:#e8446c}
.btn-lg{padding:14px 34px;font-size:1rem;border-radius:12px}
.btn-full{width:100%}
.btn-sm{padding:7px 16px;font-size:.82rem}

/* ── NAV ── */
nav.top{position:sticky;top:0;z-index:200;background:rgba(7,9,15,.88);backdrop-filter:blur(24px) saturate(160%);border-bottom:1px solid rgba(255,255,255,.07)}
.nav-inner{display:flex;align-items:center;height:68px;gap:16px}
.logo{font-family:'Cormorant Garamond',serif;font-size:1.5rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;cursor:pointer;filter:drop-shadow(0 0 16px rgba(99,91,255,.35));text-decoration:none}
.nav-links{display:flex;gap:4px;margin-left:auto}
.nav-links a{color:rgba(226,232,248,.55);padding:7px 14px;border-radius:8px;font-size:.9rem;font-weight:500;text-decoration:none;cursor:pointer;transition:all .2s}
.nav-links a:hover{color:#e2e8f8;background:rgba(255,255,255,.06)}
.nav-actions{display:flex;gap:10px;align-items:center}

/* ── SIDEBAR ── */
.app-layout{display:flex;min-height:100vh}
.sidebar{width:248px;background:rgba(255,255,255,.025);border-right:1px solid rgba(255,255,255,.07);position:fixed;top:0;left:0;height:100vh;z-index:90;display:flex;flex-direction:column;transition:transform .25s}
.sidebar-logo{font-family:'Cormorant Garamond',serif;font-size:1.3rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;padding:24px 22px 20px;border-bottom:1px solid rgba(255,255,255,.07);cursor:pointer;display:block}
.sidebar-nav{display:flex;flex-direction:column;gap:3px;padding:14px 10px;flex:1;overflow-y:auto}
.slink{display:flex;align-items:center;gap:12px;padding:11px 14px;border-radius:10px;color:rgba(226,232,248,.5);font-size:.9rem;font-weight:500;cursor:pointer;transition:all .2s;border:1px solid transparent;text-decoration:none}
.slink:hover{background:rgba(255,255,255,.05);color:#e2e8f8}
.slink.active{background:rgba(99,91,255,.15);color:#a78bfa;border-color:rgba(99,91,255,.25)}
.slink .ico{font-size:1.05rem;width:22px;text-align:center}
.sidebar-footer{padding:14px 10px;border-top:1px solid rgba(255,255,255,.07)}
.sidebar-user{padding:12px 14px;background:rgba(255,255,255,.03);border-radius:10px;margin-bottom:8px}
.sidebar-user .uname{font-size:.88rem;font-weight:600;color:#f0f4ff}
.sidebar-user .uemail{font-size:.76rem;color:rgba(226,232,248,.35);margin-top:2px}
.sidebar-user .uplan{display:inline-flex;padding:3px 10px;border-radius:50px;font-size:.72rem;font-weight:700;margin-top:6px;background:rgba(99,91,255,.15);color:#a78bfa;border:1px solid rgba(99,91,255,.2)}
.sidebar-user .uplan.pro{background:rgba(6,214,160,.12);color:#06d6a0;border-color:rgba(6,214,160,.2)}
.s-logout{display:flex;align-items:center;gap:10px;padding:10px 14px;border-radius:10px;color:rgba(226,232,248,.4);cursor:pointer;font-size:.88rem;font-family:'Outfit',sans-serif;background:none;border:1px solid rgba(255,255,255,.07);width:100%;transition:all .2s}
.s-logout:hover{background:rgba(255,79,123,.1);color:#ff4f7b;border-color:rgba(255,79,123,.2)}
.app-main{flex:1;margin-left:248px;display:flex;flex-direction:column}
.app-header{display:flex;align-items:center;gap:16px;padding:22px 32px;border-bottom:1px solid rgba(255,255,255,.07);background:rgba(7,9,15,.8);backdrop-filter:blur(20px);position:sticky;top:0;z-index:10}
.app-header h1{font-family:'Cormorant Garamond',serif;font-size:1.6rem;flex:1;color:#f0f4ff}
.app-body{padding:32px}

/* ── CARDS ── */
.card{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.08);border-radius:18px;padding:28px}
.card-glow{box-shadow:0 0 40px rgba(99,91,255,.12)}
.card-shine::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,#06d6a0,transparent)}

/* ── FORMS ── */
.fgroup{display:flex;flex-direction:column;gap:7px;margin-bottom:16px}
.fgroup label{font-size:.8rem;font-weight:700;color:rgba(226,232,248,.45);text-transform:uppercase;letter-spacing:.07em}
.fgroup input,.fgroup select,.fgroup textarea{padding:12px 16px;border:1.5px solid rgba(255,255,255,.1);border-radius:10px;background:rgba(255,255,255,.04);font-family:'Outfit',sans-serif;font-size:.95rem;color:#e2e8f8;transition:all .2s}
.fgroup input::placeholder,.fgroup textarea::placeholder{color:rgba(226,232,248,.2)}
.fgroup input:focus,.fgroup select:focus,.fgroup textarea:focus{outline:none;border-color:#635bff;background:rgba(99,91,255,.07);box-shadow:0 0 0 4px rgba(99,91,255,.1)}
.fgroup input:disabled{opacity:.45;cursor:not-allowed}
.form-row{display:grid;grid-template-columns:1fr 1fr;gap:14px}
.fcheck{display:flex;align-items:flex-start;gap:10px;font-size:.87rem;color:rgba(226,232,248,.5);margin-bottom:16px}
.fcheck input{margin-top:3px;accent-color:#635bff}
.err-msg{background:rgba(255,79,123,.12);color:#ff4f7b;border:1px solid rgba(255,79,123,.25);padding:11px 16px;border-radius:8px;font-size:.87rem;margin-bottom:14px}
.ok-msg{background:rgba(6,214,160,.1);color:#06d6a0;border:1px solid rgba(6,214,160,.25);padding:11px 16px;border-radius:8px;font-size:.87rem;margin-bottom:14px}
.warn-msg{background:rgba(255,179,71,.1);color:#ffb347;border:1px solid rgba(255,179,71,.25);padding:14px 18px;border-radius:10px;font-size:.9rem;margin-bottom:18px}

/* ── AUTH PAGES ── */
.auth-wrap{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:40px 20px;background:radial-gradient(ellipse 60% 50% at 50% 0%,rgba(99,91,255,.1) 0%,transparent 70%)}
.auth-card{background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.1);border-radius:24px;padding:44px;width:100%;max-width:450px;box-shadow:0 32px 80px rgba(0,0,0,.5);position:relative;overflow:hidden}
.auth-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,#06d6a0,transparent)}
.auth-logo{font-family:'Cormorant Garamond',serif;font-size:1.5rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;display:block;margin-bottom:28px;cursor:pointer}
.auth-card h1{font-size:1.9rem;color:#f0f4ff;margin-bottom:8px}
.auth-sub{color:rgba(226,232,248,.45);font-size:.92rem;margin-bottom:28px}
.auth-alt{margin-top:20px;font-size:.87rem;color:rgba(226,232,248,.4);text-align:center}
.auth-alt a{color:#a78bfa;cursor:pointer;text-decoration:none}
.auth-alt a:hover{color:#06d6a0}

/* ── STAT CARDS ── */
.stat-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:18px;margin-bottom:30px}
.stat-card{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:16px;padding:24px 28px;position:relative;overflow:hidden;transition:all .3s}
.stat-card::after{content:'';position:absolute;bottom:0;left:0;right:0;height:3px;background:linear-gradient(90deg,#635bff,#06d6a0);opacity:0;transition:opacity .3s}
.stat-card:hover{border-color:rgba(99,91,255,.2);box-shadow:0 0 30px rgba(99,91,255,.08)}
.stat-card:hover::after{opacity:1}
.stat-label{font-size:.74rem;font-weight:700;color:rgba(226,232,248,.4);text-transform:uppercase;letter-spacing:.1em;margin-bottom:10px}
.stat-val{font-family:'Cormorant Garamond',serif;font-size:2.4rem;font-weight:700;background:linear-gradient(135deg,#f0f4ff,#a78bfa);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}

/* ── UPGRADE BANNER ── */
.upgrade-banner{background:linear-gradient(135deg,rgba(99,91,255,.15),rgba(6,214,160,.07));border:1px solid rgba(99,91,255,.3);border-radius:14px;padding:18px 24px;display:flex;align-items:center;gap:18px;flex-wrap:wrap;margin-bottom:28px}
.upgrade-banner-txt strong{display:block;color:#a78bfa;font-size:.95rem;margin-bottom:2px}
.upgrade-banner-txt span{font-size:.83rem;color:rgba(226,232,248,.45)}

/* ── DOCS TABLE ── */
.doc-list{display:flex;flex-direction:column;gap:10px}
.doc-row{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px;padding:16px 20px;display:flex;align-items:center;gap:16px;transition:all .2s}
.doc-row:hover{border-color:rgba(99,91,255,.2);background:rgba(99,91,255,.04)}
.doc-icon{font-size:1.7rem;filter:drop-shadow(0 0 8px rgba(99,91,255,.3))}
.doc-info{flex:1}
.doc-name{font-weight:600;font-size:.92rem;color:#f0f4ff}
.doc-meta{font-size:.78rem;color:rgba(226,232,248,.35);margin-top:2px}
.badge{padding:4px 12px;border-radius:50px;font-size:.74rem;font-weight:700}
.badge-green{background:rgba(6,214,160,.12);color:#06d6a0;border:1px solid rgba(6,214,160,.25)}
.badge-amber{background:rgba(255,179,71,.12);color:#ffb347;border:1px solid rgba(255,179,71,.25)}
.badge-red{background:rgba(255,79,123,.12);color:#ff4f7b;border:1px solid rgba(255,79,123,.25)}
.badge-purple{background:rgba(99,91,255,.15);color:#a78bfa;border:1px solid rgba(99,91,255,.25)}

/* ── SIGN STEPS ── */
.sign-step{background:rgba(255,255,255,.025);border:1px solid rgba(255,255,255,.07);border-radius:18px;padding:28px;margin-bottom:20px;transition:border-color .3s}
.sign-step:hover{border-color:rgba(99,91,255,.15)}
.step-hdr{display:flex;align-items:center;gap:14px;margin-bottom:20px}
.step-num{width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,#635bff,#4f46e5);color:#fff;display:flex;align-items:center;justify-content:center;font-weight:700;font-size:.88rem;flex-shrink:0;box-shadow:0 4px 12px rgba(99,91,255,.4)}
.step-hdr h2{font-size:1.1rem;font-weight:700;color:#f0f4ff;font-family:'Outfit',sans-serif}

/* ── DROP ZONE ── */
.drop-zone{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;border:2px dashed rgba(255,255,255,.12);border-radius:14px;padding:48px 20px;cursor:pointer;text-align:center;transition:all .2s;background:rgba(255,255,255,.02)}
.drop-zone:hover,.drop-zone.over{border-color:#635bff;background:rgba(99,91,255,.07);box-shadow:inset 0 0 30px rgba(99,91,255,.05)}
.drop-icon{font-size:2.4rem;filter:drop-shadow(0 0 10px rgba(99,91,255,.4))}
.drop-zone p{font-size:.9rem;color:rgba(226,232,248,.4)}
.drop-zone strong{color:#a78bfa}

/* ── TABS ── */
.tab-bar{display:flex;gap:4px;margin-bottom:18px;border-bottom:1px solid rgba(255,255,255,.07);padding-bottom:10px}
.tab{background:none;border:none;padding:8px 18px;border-radius:8px;cursor:pointer;font-size:.88rem;color:rgba(226,232,248,.4);font-family:'Outfit',sans-serif;font-weight:500;transition:all .2s}
.tab:hover{background:rgba(255,255,255,.05);color:#e2e8f8}
.tab.active{background:rgba(99,91,255,.15);color:#a78bfa;border:1px solid rgba(99,91,255,.25)}
.tab-panel{display:flex;flex-direction:column;gap:12px}

/* ── CANVAS + CAMERA ── */
#sig-canvas{display:block;width:100%;max-width:500px;height:160px;border:1.5px solid rgba(255,255,255,.1);border-radius:10px;background:#0d1120;cursor:crosshair;touch-action:none;transition:border-color .2s}
#sig-canvas:hover{border-color:#635bff;box-shadow:0 0 0 3px rgba(99,91,255,.1)}
.swatches{display:flex;gap:8px}
.swatch{width:26px;height:26px;border-radius:50%;cursor:pointer;border:2.5px solid transparent;transition:all .2s;box-shadow:0 2px 8px rgba(0,0,0,.4)}
.swatch.active{border-color:#fff;transform:scale(1.15)}
.cam-wrap{background:#000;border-radius:10px;overflow:hidden;max-width:480px;border:1px solid rgba(255,255,255,.1);position:relative}
.cam-wrap video{width:100%;display:block}
.sig-ready-banner{padding:12px 18px;background:rgba(6,214,160,.1);color:#06d6a0;border:1px solid rgba(6,214,160,.25);border-radius:8px;font-size:.88rem;font-weight:600;margin-top:12px}

/* ── AI PLACEMENT ── */
.ai-input-wrap{display:flex;gap:10px;align-items:center;margin-bottom:14px;flex-wrap:wrap}
.ai-chip{padding:7px 16px;border-radius:50px;background:rgba(99,91,255,.12);border:1px solid rgba(99,91,255,.25);color:#a78bfa;font-size:.82rem;cursor:pointer;transition:all .2s}
.ai-chip:hover{background:rgba(99,91,255,.2);border-color:rgba(99,91,255,.4)}

/* ── PDF VIEWER ── */
.pdf-viewer-wrap{position:relative;border:1px solid rgba(255,255,255,.1);border-radius:10px;overflow:hidden;background:#0a0a1a;line-height:0;max-width:700px}
#pdf-canvas{display:block;width:100%;height:auto}
.sig-overlay{position:absolute;border:2px dashed #635bff;border-radius:4px;cursor:grab;user-select:none;box-shadow:0 0 0 1px rgba(99,91,255,.3),0 4px 20px rgba(0,0,0,.5)}
.sig-overlay:active{cursor:grabbing}
.sig-overlay img{display:block;width:100%;height:100%;object-fit:contain;pointer-events:none}
.resize-handle{position:absolute;bottom:-8px;right:-8px;width:18px;height:18px;background:linear-gradient(135deg,#635bff,#06d6a0);border-radius:50%;cursor:se-resize;border:2px solid #07090f;box-shadow:0 2px 8px rgba(99,91,255,.4)}
.ai-processing{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:14px;padding:40px;text-align:center}
.ai-spinner{width:48px;height:48px;border:3px solid rgba(99,91,255,.2);border-top-color:#635bff;border-radius:50%;animation:spin 1s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.ai-label{color:#a78bfa;font-weight:600}
.sign-actions{display:flex;align-items:center;gap:16px;flex-wrap:wrap;margin-top:20px}
.status-lbl{font-size:.87rem;color:rgba(226,232,248,.4)}
.status-lbl.ok{color:#06d6a0}
.status-lbl.err{color:#ff4f7b}

/* ── PRICING CARDS ── */
.pricing-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:22px;max-width:960px;margin:0 auto}
.price-card{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.08);border-radius:24px;padding:36px;transition:all .3s;position:relative;overflow:hidden}
.price-card:hover{border-color:rgba(99,91,255,.3);transform:translateY(-4px);box-shadow:0 30px 60px rgba(0,0,0,.4)}
.price-card.feat{background:linear-gradient(145deg,rgba(99,91,255,.18),rgba(6,214,160,.07));border:1.5px solid rgba(99,91,255,.5);box-shadow:0 0 60px rgba(99,91,255,.18)}
.price-card.feat::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,#06d6a0,transparent)}
.price-badge{position:absolute;top:-13px;left:50%;transform:translateX(-50%);background:linear-gradient(135deg,#635bff,#06d6a0);color:#fff;font-size:.74rem;font-weight:700;padding:5px 20px;border-radius:50px;white-space:nowrap;box-shadow:0 4px 20px rgba(99,91,255,.4)}
.plan-name{font-size:.78rem;font-weight:700;color:rgba(226,232,248,.4);text-transform:uppercase;letter-spacing:.12em;margin-bottom:14px}
.plan-price{display:flex;align-items:baseline;gap:4px;margin-bottom:6px}
.plan-amt{font-family:'Cormorant Garamond',serif;font-size:3.4rem;font-weight:700;background:linear-gradient(135deg,#f0f4ff,#a78bfa);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;line-height:1}
.feat .plan-amt{background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.plan-per{font-size:.9rem;color:rgba(226,232,248,.4)}
.plan-desc{font-size:.85rem;color:rgba(226,232,248,.4);margin-bottom:24px;padding-bottom:22px;border-bottom:1px solid rgba(255,255,255,.07)}
.plan-feats{list-style:none;margin-bottom:28px;display:flex;flex-direction:column;gap:10px}
.plan-feats li{display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)}
.plan-feats li.off{opacity:.3;text-decoration:line-through}
.chk{width:20px;height:20px;border-radius:50%;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:.7rem;font-weight:700}
.chk-y{background:rgba(6,214,160,.18);color:#06d6a0;border:1px solid rgba(6,214,160,.3)}
.chk-n{background:rgba(255,255,255,.04);color:rgba(226,232,248,.2);border:1px solid rgba(255,255,255,.08)}

/* ── PAYMENT PAGE ── */
.pay-layout{display:grid;grid-template-columns:1fr 1.1fr;gap:32px;max-width:900px;margin:0 auto}
.pay-summary{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.08);border-radius:18px;padding:28px;align-self:start}
.pay-form-card{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.1);border-radius:18px;padding:32px;position:relative;overflow:hidden}
.pay-form-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,#06d6a0,transparent)}
.pay-methods{display:flex;flex-direction:column;gap:10px;margin-bottom:18px}
.pay-btn{display:flex;align-items:center;gap:12px;padding:13px 18px;border-radius:10px;border:1.5px solid rgba(255,255,255,.1);background:rgba(255,255,255,.04);color:#e2e8f8;cursor:pointer;font-family:'Outfit',sans-serif;font-size:.9rem;font-weight:600;transition:all .2s}
.pay-btn:hover{border-color:#635bff;background:rgba(99,91,255,.08)}
.or-div{text-align:center;color:rgba(226,232,248,.3);font-size:.8rem;position:relative;margin:6px 0}
.or-div::before,.or-div::after{content:'';position:absolute;top:50%;width:44%;height:1px;background:rgba(255,255,255,.08)}
.or-div::before{left:0}.or-div::after{right:0}
.secure-note{display:flex;align-items:center;justify-content:center;gap:6px;margin-top:14px;font-size:.75rem;color:rgba(226,232,248,.3)}
/* ── BACK BUTTON ── */
.back-btn{display:inline-flex;align-items:center;gap:8px;padding:9px 18px;border-radius:10px;background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.1);color:rgba(226,232,248,.6);font-size:.88rem;font-weight:500;cursor:pointer;transition:all .2s;font-family:'Outfit',sans-serif;text-decoration:none}
.back-btn:hover{background:rgba(255,255,255,.1);color:#e2e8f8;border-color:rgba(255,255,255,.2);transform:translateX(-2px)}
.back-btn::before{content:'←';font-size:1rem}
.trial-note{background:rgba(6,214,160,.08);border:1px solid rgba(6,214,160,.2);border-radius:10px;padding:14px 18px;margin-bottom:20px;font-size:.88rem;color:#06d6a0}

/* ── LANDING SECTIONS ── */
.hero{min-height:92vh;display:flex;align-items:center;padding:80px 0 60px}
.hero-inner{display:grid;grid-template-columns:1fr 1fr;gap:80px;align-items:center}
.hero-badge{display:inline-flex;align-items:center;gap:8px;background:rgba(99,91,255,.15);border:1px solid rgba(99,91,255,.35);color:#a78bfa;font-size:.82rem;font-weight:600;padding:7px 18px;border-radius:50px;margin-bottom:28px}
.hero-badge::before{content:'';width:8px;height:8px;border-radius:50%;background:#06d6a0;box-shadow:0 0 8px #06d6a0;animation:blink 2s infinite}
@keyframes blink{0%,100%{opacity:1}50%{opacity:.3}}
.hero h1{font-size:clamp(3rem,5.5vw,5rem);letter-spacing:-.03em;color:#f0f4ff;margin-bottom:22px}
.hero-sub{font-size:1.1rem;color:rgba(226,232,248,.55);max-width:480px;margin-bottom:38px;line-height:1.75}
.hero-actions{display:flex;gap:14px;flex-wrap:wrap;margin-bottom:48px}
.hero-stats{display:flex;background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.08);border-radius:14px;overflow:hidden}
.hstat{flex:1;padding:18px 20px;text-align:center;border-right:1px solid rgba(255,255,255,.08)}
.hstat:last-child{border-right:none}
.hstat-n{font-family:'Cormorant Garamond',serif;font-size:1.7rem;background:linear-gradient(135deg,#f0f4ff,#a78bfa);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;display:block}
.hstat-l{font-size:.74rem;color:rgba(226,232,248,.4);display:block;margin-top:2px}
.doc-preview{background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.1);border-radius:22px;padding:28px;box-shadow:0 32px 80px rgba(0,0,0,.5);position:relative;overflow:hidden}
.doc-preview::before{content:'';position:absolute;top:0;left:0;right:0;height:1px;background:linear-gradient(90deg,transparent,rgba(167,139,250,.6),rgba(6,214,160,.6),transparent)}
.dp-header{display:flex;align-items:center;gap:12px;margin-bottom:20px;padding-bottom:16px;border-bottom:1px solid rgba(255,255,255,.07)}
.dp-icon{width:40px;height:40px;border-radius:10px;background:linear-gradient(135deg,rgba(99,91,255,.3),rgba(6,214,160,.2));border:1px solid rgba(99,91,255,.3);display:flex;align-items:center;justify-content:center;font-size:1.2rem}
.dp-lines{display:flex;flex-direction:column;gap:8px;margin-bottom:18px}
.dp-line{height:9px;border-radius:5px;background:rgba(255,255,255,.07)}
.dp-sig{border:2px dashed rgba(99,91,255,.4);border-radius:10px;padding:14px;display:flex;align-items:center;gap:10px;margin-bottom:10px;background:rgba(99,91,255,.05)}
.dp-sig-done{border:1px solid rgba(6,214,160,.4);border-radius:10px;padding:14px;display:flex;align-items:center;gap:10px;background:rgba(6,214,160,.06)}
.dp-sig-bar{width:72px;height:28px;border-radius:6px;background:linear-gradient(135deg,rgba(6,214,160,.35),rgba(99,91,255,.25));border:1px solid rgba(6,214,160,.3)}
.section-lbl{display:inline-flex;align-items:center;gap:8px;color:#a78bfa;font-size:.76rem;font-weight:700;text-transform:uppercase;letter-spacing:.12em;margin-bottom:20px}
.section-lbl::before{content:'';width:22px;height:2px;background:linear-gradient(90deg,#a78bfa,transparent)}
.section-h{font-size:clamp(2rem,4vw,3rem);font-weight:700;color:#f0f4ff;margin-bottom:14px;letter-spacing:-.02em}
.section-sub{color:rgba(226,232,248,.45);font-size:1rem;max-width:500px;margin-bottom:60px}
.steps-wrap{padding:90px 0;background:linear-gradient(180deg,transparent,rgba(99,91,255,.04) 50%,transparent);border-top:1px solid rgba(255,255,255,.05);border-bottom:1px solid rgba(255,255,255,.05)}
.steps-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:22px}
.step-card{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:18px;padding:28px;transition:all .3s}
.step-card:hover{border-color:rgba(99,91,255,.3);transform:translateY(-4px);box-shadow:0 0 36px rgba(99,91,255,.12)}
.step-n{font-family:'Cormorant Garamond',serif;font-size:3.5rem;font-weight:700;background:linear-gradient(135deg,rgba(99,91,255,.5),rgba(6,214,160,.3));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:16px;line-height:1}
.step-ico{width:48px;height:48px;border-radius:12px;background:linear-gradient(135deg,rgba(99,91,255,.25),rgba(6,214,160,.15));border:1px solid rgba(99,91,255,.3);display:flex;align-items:center;justify-content:center;font-size:1.4rem;margin-bottom:16px;box-shadow:0 0 18px rgba(99,91,255,.2)}
.step-card h3{font-size:1.05rem;font-weight:700;color:#f0f4ff;margin-bottom:8px;font-family:'Outfit',sans-serif}
.step-card p{font-size:.88rem;color:rgba(226,232,248,.45);line-height:1.7}
.feats-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}
.feat-card{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:16px;padding:26px;transition:all .3s}
.feat-card:hover{border-color:rgba(99,91,255,.25);transform:translateY(-3px);box-shadow:0 20px 40px rgba(0,0,0,.3)}
.feat-ico{font-size:1.9rem;margin-bottom:14px;display:block}
.feat-card h3{font-size:1rem;font-weight:700;color:#f0f4ff;margin-bottom:8px;font-family:'Outfit',sans-serif}
.feat-card p{font-size:.86rem;color:rgba(226,232,248,.45);line-height:1.7}
.testi-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.testi{background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:16px;padding:26px;transition:all .3s}
.testi:hover{border-color:rgba(99,91,255,.2);transform:translateY(-2px)}
.stars{color:#fbbf24;letter-spacing:2px;margin-bottom:12px}
.testi p{font-size:.9rem;color:rgba(226,232,248,.6);line-height:1.75;font-style:italic;margin-bottom:18px}
.t-author{display:flex;align-items:center;gap:10px}
.t-avatar{width:36px;height:36px;border-radius:50%;background:linear-gradient(135deg,#635bff,#06d6a0);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:.82rem;color:#fff;flex-shrink:0}
.t-name{font-weight:600;font-size:.86rem;color:#f0f4ff}
.t-role{font-size:.76rem;color:rgba(226,232,248,.35)}
.cta-sec{padding:80px 0}
.cta-box{background:linear-gradient(135deg,rgba(99,91,255,.2),rgba(6,214,160,.1));border:1px solid rgba(99,91,255,.3);border-radius:26px;padding:56px;text-align:center;position:relative;overflow:hidden}
.cta-box::before{content:'';position:absolute;top:-80px;left:50%;transform:translateX(-50%);width:350px;height:350px;border-radius:50%;background:radial-gradient(circle,rgba(99,91,255,.12) 0%,transparent 70%);pointer-events:none}
.cta-box h2{font-size:clamp(1.8rem,4vw,2.8rem);color:#f0f4ff;margin-bottom:12px;letter-spacing:-.02em}
.cta-box p{color:rgba(226,232,248,.5);font-size:1rem;max-width:480px;margin:0 auto 32px}
.cta-btns{display:flex;gap:12px;justify-content:center;flex-wrap:wrap}
footer{border-top:1px solid rgba(255,255,255,.07);padding:56px 0 28px;background:rgba(0,0,0,.3)}
.footer-inner{display:flex;gap:48px;flex-wrap:wrap;margin-bottom:44px}
.footer-brand{max-width:240px}
.f-logo{font-family:'Cormorant Garamond',serif;font-size:1.4rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;display:block;margin-bottom:10px;cursor:pointer}
.footer-brand p{font-size:.84rem;color:rgba(226,232,248,.35);line-height:1.7}
.footer-links{display:flex;gap:52px;flex:1;flex-wrap:wrap}
.fcol{display:flex;flex-direction:column;gap:10px}
.fcol h4{color:rgba(226,232,248,.8);font-size:.78rem;font-weight:700;text-transform:uppercase;letter-spacing:.1em;margin-bottom:4px}
.fcol a{color:rgba(226,232,248,.35);font-size:.86rem;text-decoration:none;cursor:pointer;transition:color .2s}
.fcol a:hover{color:#a78bfa}
.footer-btm{border-top:1px solid rgba(255,255,255,.06);padding-top:22px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:10px;font-size:.8rem;color:rgba(226,232,248,.25)}
.empty-state{text-align:center;padding:56px 20px;color:rgba(226,232,248,.3)}
.empty-state .e-ico{font-size:2.8rem;margin-bottom:14px;display:block}
.loading-txt{text-align:center;padding:40px;color:rgba(226,232,248,.35);font-size:.9rem}
.section-hdr{display:flex;align-items:center;justify-content:space-between;margin-bottom:18px}
.section-hdr h2{font-family:'Cormorant Garamond',serif;font-size:1.25rem;color:#f0f4ff}
@media(max-width:900px){
  .hero-inner,.pay-layout{grid-template-columns:1fr}
  .doc-preview{display:none}
  .steps-grid,.feats-grid,.pricing-grid,.testi-grid{grid-template-columns:1fr}
  .app-layout{flex-direction:column}
  .sidebar{width:100%;height:auto;position:relative;transform:none}
  .app-main{margin-left:0}
}
@media(max-width:600px){
  .wrap{padding:0 18px}
  .app-body{padding:18px}
  .hero-actions,.cta-btns{flex-direction:column}
  .hero-stats{flex-direction:column}
  .hstat{border-right:none;border-bottom:1px solid rgba(255,255,255,.07)}
  .hstat:last-child{border-bottom:none}
  .nav-links{display:none}
  .cta-box{padding:36px 22px}
  .form-row{grid-template-columns:1fr}
}
</style>
</head>
<body>

<!-- ═══════════════════════════════════════
     PAGE: HOME
═══════════════════════════════════════ -->
<div id="pg-home" class="page active">
<nav class="top"><div class="wrap nav-inner">
  <span class="logo" onclick="go('home')">SignForge</span>
  <div class="nav-links">
    <a onclick="go('features')">Features</a>
    <a onclick="go('pricing')">Pricing</a>
    <a onclick="go('how')">How it works</a>
    <a onclick="go('contact')">Contact</a>
  </div>
  <div class="nav-actions">
    <button class="btn btn-ghost" onclick="go('login')">Log in</button>
    <button class="btn btn-primary" onclick="go('register')">Get Started Free →</button>
  </div>
</div></nav>

<section class="hero"><div class="wrap"><div class="hero-inner">
  <div>
    <div class="hero-badge">AI-Powered PDF Signing</div>
    <h1>Sign documents<br/>the <em class="grad">intelligent</em><br/>way.</h1>
    <p class="hero-sub">Upload any PDF, capture your handwritten signature with your camera, tell AI where to place it — signed document ready in 30 seconds.</p>
    <div class="hero-actions">
      <button class="btn btn-primary btn-lg" onclick="go('register')">Start free — 1 document →</button>
      <button class="btn btn-ghost btn-lg" onclick="go('how')">See how it works</button>
    </div>
    <div class="hero-stats">
      <div class="hstat"><span class="hstat-n">10K+</span><span class="hstat-l">Documents signed</span></div>
      <div class="hstat"><span class="hstat-n">4.9★</span><span class="hstat-l">User rating</span></div>
      <div class="hstat"><span class="hstat-n">30s</span><span class="hstat-l">Average time</span></div>
    </div>
  </div>
  <div class="doc-preview">
    <div class="dp-header"><div class="dp-icon">📄</div><div><div style="font-weight:600;font-size:.92rem;color:#f0f4ff">Contract_Final.pdf</div><div style="font-size:.76rem;color:rgba(226,232,248,.4);margin-top:2px">3 pages · AI signature ready</div></div></div>
    <div class="dp-lines"><div class="dp-line" style="width:100%"></div><div class="dp-line" style="width:82%"></div><div class="dp-line" style="width:100%"></div><div class="dp-line" style="width:64%"></div><div class="dp-line" style="width:90%"></div></div>
    <div class="dp-sig">✍️ <span style="font-size:.84rem;color:rgba(226,232,248,.5)">AI detected signature zone</span></div>
    <div class="dp-sig-done"><div class="dp-sig-bar"></div><span style="font-size:.82rem;color:#06d6a0;font-weight:600">✓ Signature placed by AI</span></div>
    <div style="display:inline-flex;align-items:center;gap:6px;background:rgba(6,214,160,.12);color:#06d6a0;border:1px solid rgba(6,214,160,.3);font-size:.74rem;font-weight:600;padding:4px 12px;border-radius:50px;margin-top:10px"><span style="width:6px;height:6px;border-radius:50%;background:#06d6a0;box-shadow:0 0 6px #06d6a0;animation:blink 2s infinite;display:inline-block"></span>Ready to download</div>
  </div>
</div></div></section>

<section class="steps-wrap" id="how-section"><div class="wrap">
  <div class="section-lbl">How it works</div>
  <h2 class="section-h">Three steps. Done in 30 seconds.</h2>
  <p class="section-sub">No printing. No scanning. No fax machines. Ever again.</p>
  <div class="steps-grid">
    <div class="step-card"><div class="step-n">01</div><div class="step-ico">📄</div><h3>Upload your PDF</h3><p>Drag and drop any PDF up to 20MB. Contracts, agreements, invoices — anything works.</p></div>
    <div class="step-card"><div class="step-n">02</div><div class="step-ico">📷</div><h3>Capture your signature</h3><p>Use your phone camera to photograph your handwritten signature, or draw it directly on screen.</p></div>
    <div class="step-card"><div class="step-n">03</div><div class="step-ico">🤖</div><h3>AI places it perfectly</h3><p>Tell the AI "bottom right" or "above my printed name" — it figures out exactly where to put it.</p></div>
  </div>
</div></section>

<section style="padding:90px 0"><div class="wrap">
  <div class="section-lbl">Features</div>
  <h2 class="section-h">Everything you need.</h2>
  <p class="section-sub">Built for professionals who sign documents every day.</p>
  <div class="feats-grid">
    <div class="feat-card"><span class="feat-ico">🤖</span><h3>AI Placement</h3><p>Just type where you want the signature. The AI understands natural language like "bottom right" or "above my name".</p></div>
    <div class="feat-card"><span class="feat-ico">📷</span><h3>Camera Capture</h3><p>Hold your signed paper up to your phone camera. We capture, crop, and clean the signature automatically.</p></div>
    <div class="feat-card"><span class="feat-ico">✍️</span><h3>Draw on Screen</h3><p>Sign directly with your finger or mouse on any device. Colour options included.</p></div>
    <div class="feat-card"><span class="feat-ico">📄</span><h3>Any Document</h3><p>PDFs, images, contracts, invoices. Upload anything up to 20MB and sign it in seconds.</p></div>
    <div class="feat-card"><span class="feat-ico">🔒</span><h3>Secure & Private</h3><p>HTTPS end-to-end. Files deleted within 24 hours. No tracking. Fully GDPR compliant.</p></div>
    <div class="feat-card"><span class="feat-ico">⬇️</span><h3>Instant Download</h3><p>Your professionally signed document is ready to download the moment you confirm placement.</p></div>
  </div>
</div></section>

<section style="padding:80px 0;background:rgba(99,91,255,.03);border-top:1px solid rgba(255,255,255,.05);border-bottom:1px solid rgba(255,255,255,.05)"><div class="wrap">
  <div class="section-lbl">Testimonials</div>
  <h2 class="section-h" style="margin-bottom:48px">People love SignForge.</h2>
  <div class="testi-grid">
    <div class="testi"><div class="stars">★★★★★</div><p>"I used to spend 20 minutes printing, signing, scanning. Now I do it in 30 seconds. Genuinely life-changing for my workflow."</p><div class="t-author"><div class="t-avatar">SJ</div><div><div class="t-name">Sarah Johnson</div><div class="t-role">Freelance Consultant</div></div></div></div>
    <div class="testi"><div class="stars">★★★★★</div><p>"The AI placement is incredible. I just type 'bottom right' and it's done perfectly every time. Worth every penny."</p><div class="t-author"><div class="t-avatar" style="background:linear-gradient(135deg,#06d6a0,#059669)">MK</div><div><div class="t-name">Marcus Kim</div><div class="t-role">Real Estate Agent</div></div></div></div>
    <div class="testi"><div class="stars">★★★★★</div><p>"At $4.99 a month this is a steal. I sign dozens of documents weekly. The camera signature feature is amazing."</p><div class="t-author"><div class="t-avatar" style="background:linear-gradient(135deg,#ff489a,#9333ea)">AT</div><div><div class="t-name">Aisha Tran</div><div class="t-role">Business Owner</div></div></div></div>
  </div>
</div></section>

<section class="cta-sec"><div class="wrap"><div class="cta-box">
  <h2>Ready to stop printing<br/>and scanning forever?</h2>
  <p>Join thousands of professionals. Your first document is completely free.</p>
  <div class="cta-btns">
    <button class="btn btn-primary btn-lg" onclick="go('register')">Create free account →</button>
    <button class="btn btn-ghost btn-lg" onclick="go('pricing')">View pricing</button>
  </div>
</div></div></section>

<footer><div class="wrap">
  <div class="footer-inner">
    <div class="footer-brand"><span class="f-logo" onclick="go('home')">SignForge</span><p>AI-powered document signing for professionals. Simple, secure, instant.</p></div>
    <div class="footer-links">
      <div class="fcol"><h4>Product</h4><a onclick="go('features')">Features</a><a onclick="go('how')">How it works</a><a onclick="go('pricing')">Pricing</a></div>
      <div class="fcol"><h4>Account</h4><a onclick="go('login')">Log in</a><a onclick="go('register')">Sign up free</a><a onclick="go('dashboard')">Dashboard</a></div>
      <div class="fcol"><h4>Company</h4><a onclick="go('contact')">Contact</a><a onclick="go('info')">How it works</a><a onclick="go('terms')">Terms of Service</a><a onclick="go('privacy')">Privacy Policy</a></div>
    </div>
  </div>
  <div class="footer-btm"><span>© <span id="yr"></span> SignForge. All rights reserved.</span><span>Built with ❤️ for professionals</span></div>
</div></footer>
</div>

<!-- ═══════════════════════════════════════
     PAGE: FEATURES
═══════════════════════════════════════ -->
<div id="pg-features" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-links"><a onclick="go('features')">Features</a><a onclick="go('pricing')">Pricing</a><a onclick="go('how')">How it works</a></div><div class="nav-actions"><button class="back-btn" onclick="go('home')">Back to Home</button><button class="btn btn-ghost" onclick="go('login')">Log in</button><button class="btn btn-primary" onclick="go('register')">Get Started →</button></div></div></nav>
<div style="padding:80px 0"><div class="wrap">
  <div style="text-align:center;margin-bottom:64px"><div class="section-lbl" style="justify-content:center">Everything included</div><h2 class="section-h" style="font-size:clamp(2.2rem,5vw,3.5rem)">Features built for<br/><em class="grad">professionals.</em></h2></div>
  <div class="feats-grid" style="gap:22px">
    <div class="feat-card"><span class="feat-ico">🤖</span><h3>AI Signature Placement</h3><p>Type a plain English instruction like "Place signature bottom right" or "Sign above my printed name". The AI processes your instruction and positions the signature precisely.</p></div>
    <div class="feat-card"><span class="feat-ico">📷</span><h3>Camera Capture</h3><p>Works on iOS and Android. Hold your handwritten signature up to the camera. We automatically detect, crop, and clean the background for a professional result.</p></div>
    <div class="feat-card"><span class="feat-ico">✍️</span><h3>Draw Signature</h3><p>Use your mouse, trackpad, or finger. Multiple colour options. Touch-optimised for tablets and phones. Save your signature for future use.</p></div>
    <div class="feat-card"><span class="feat-ico">📄</span><h3>Multi-Format Support</h3><p>PDF, JPG, PNG — any document format works. Upload files up to 20MB. Preview every page before signing.</p></div>
    <div class="feat-card"><span class="feat-ico">🎯</span><h3>Manual Placement</h3><p>Not trusting AI? Drag and drop your signature exactly where you want it. Resize with the corner handle. Pixel-perfect control.</p></div>
    <div class="feat-card"><span class="feat-ico">💾</span><h3>Saved Signatures</h3><p>Sign once, use forever. Your signature is securely saved to your profile. Never draw it again.</p></div>
    <div class="feat-card"><span class="feat-ico">📋</span><h3>Document History</h3><p>Every document you've signed is logged in your dashboard. Re-download within 24 hours. Full audit trail.</p></div>
    <div class="feat-card"><span class="feat-ico">🔒</span><h3>Bank-Grade Security</h3><p>256-bit SSL encryption. Files automatically deleted after 24 hours. GDPR compliant. We never store your documents permanently.</p></div>
    <div class="feat-card"><span class="feat-ico">📱</span><h3>Works Everywhere</h3><p>Desktop, tablet, mobile — fully responsive. No app to download. Works in any modern browser.</p></div>
  </div>
  <div style="text-align:center;margin-top:64px"><button class="btn btn-primary btn-lg" onclick="go('register')">Try it free — no credit card →</button></div>
</div></div>
<footer><div class="wrap"><div class="footer-btm" style="border-top:1px solid rgba(255,255,255,.06);padding-top:22px"><span>© <span class="yr2"></span> SignForge</span><a onclick="go('home')" style="color:rgba(226,232,248,.3);text-decoration:none;cursor:pointer">Back to home</a></div></div></footer>
</div>

<!-- ═══════════════════════════════════════
     PAGE: PRICING
═══════════════════════════════════════ -->
<div id="pg-pricing" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-links"><a onclick="go('features')">Features</a><a onclick="go('pricing')">Pricing</a><a onclick="go('how')">How it works</a></div><div class="nav-actions"><button class="back-btn" onclick="go('home')">Back to Home</button><button class="btn btn-ghost" onclick="go('login')">Log in</button><button class="btn btn-primary" onclick="go('register')">Get Started →</button></div></div></nav>
<div style="padding:80px 0"><div class="wrap">
  <div style="text-align:center;margin-bottom:16px"><div class="section-lbl" style="justify-content:center">Simple pricing</div><h2 class="section-h" style="font-size:clamp(2.2rem,5vw,3.5rem)">Honest pricing.<br/><em class="grad">No surprises.</em></h2><p style="color:rgba(226,232,248,.45);font-size:1rem;max-width:480px;margin:0 auto 60px">Start free. Upgrade when you need more. Cancel anytime with one click.</p></div>
  <div class="pricing-grid">
    <div class="price-card">
      <div class="plan-name">Free</div>
      <div class="plan-price"><span class="plan-amt">$0</span><span class="plan-per">/month</span></div>
      <p class="plan-desc">Perfect for trying SignForge</p>
      <ul class="plan-feats">
        <li><span class="chk chk-y">✓</span>1 document signing (trial)</li>
        <li><span class="chk chk-y">✓</span>Camera signature capture</li>
        <li><span class="chk chk-y">✓</span>Draw signature</li>
        <li><span class="chk chk-y">✓</span>AI placement</li>
        <li class="off"><span class="chk chk-n">✕</span>Document history</li>
        <li class="off"><span class="chk chk-n">✕</span>Saved signatures</li>
        <li class="off"><span class="chk chk-n">✕</span>Priority support</li>
      </ul>
      <button class="btn btn-ghost btn-full" onclick="go('register')">Start free trial</button>
    </div>
    <div class="price-card feat">
      <div class="price-badge">⚡ Most Popular</div>
      <div class="plan-name">Starter</div>
      <div class="plan-price"><span class="plan-amt">$1.99</span><span class="plan-per">/month</span></div>
      <p class="plan-desc">For regular document signers</p>
      <ul class="plan-feats">
        <li><span class="chk chk-y">✓</span>10 documents/month</li>
        <li><span class="chk chk-y">✓</span>Camera signature capture</li>
        <li><span class="chk chk-y">✓</span>Draw signature</li>
        <li><span class="chk chk-y">✓</span>AI placement</li>
        <li><span class="chk chk-y">✓</span>Document history</li>
        <li><span class="chk chk-y">✓</span>Saved signatures</li>
        <li class="off"><span class="chk chk-n">✕</span>Priority support</li>
      </ul>
      <button class="btn btn-primary btn-full" onclick="go('checkout','starter')">Get Starter →</button>
    </div>
    <div class="price-card">
      <div class="plan-name">Pro</div>
      <div class="plan-price"><span class="plan-amt">$4.99</span><span class="plan-per">/month</span></div>
      <p class="plan-desc">For power users & businesses</p>
      <ul class="plan-feats">
        <li><span class="chk chk-y">✓</span><strong>Unlimited documents</strong></li>
        <li><span class="chk chk-y">✓</span>Camera signature capture</li>
        <li><span class="chk chk-y">✓</span>Draw signature</li>
        <li><span class="chk chk-y">✓</span>AI placement</li>
        <li><span class="chk chk-y">✓</span>Document history</li>
        <li><span class="chk chk-y">✓</span>Saved signatures</li>
        <li><span class="chk chk-y">✓</span>Priority support</li>
      </ul>
      <button class="btn btn-emerald btn-full" onclick="go('checkout','pro')">Get Pro →</button>
    </div>
  </div>
  <div style="text-align:center;margin-top:48px;color:rgba(226,232,248,.35);font-size:.88rem">🔒 Secured by Stripe · Cancel anytime · No hidden fees</div>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: HOW IT WORKS
═══════════════════════════════════════ -->
<div id="pg-how" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-links"><a onclick="go('features')">Features</a><a onclick="go('pricing')">Pricing</a><a onclick="go('how')">How it works</a></div><div class="nav-actions"><button class="back-btn" onclick="go('home')">Back to Home</button><button class="btn btn-ghost" onclick="go('login')">Log in</button><button class="btn btn-primary" onclick="go('register')">Get Started →</button></div></div></nav>
<div style="padding:80px 0"><div class="wrap">
  <div style="text-align:center;margin-bottom:64px"><div class="section-lbl" style="justify-content:center">The process</div><h2 class="section-h" style="font-size:clamp(2.2rem,5vw,3.5rem)">How SignForge works</h2></div>
  <div style="max-width:700px;margin:0 auto;display:flex;flex-direction:column;gap:28px">
    <div class="card" style="display:flex;gap:20px;align-items:flex-start"><div style="width:48px;height:48px;border-radius:50%;background:linear-gradient(135deg,#635bff,#4f46e5);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:1.1rem;flex-shrink:0;box-shadow:0 4px 16px rgba(99,91,255,.4);color:#fff">1</div><div><h3 style="font-size:1.1rem;margin-bottom:8px;font-family:'Outfit',sans-serif;font-weight:700;color:#f0f4ff">Create your free account</h3><p style="color:rgba(226,232,248,.5);font-size:.9rem;line-height:1.7">Sign up with your email. No credit card required. You get 1 free document signing immediately.</p></div></div>
    <div class="card" style="display:flex;gap:20px;align-items:flex-start"><div style="width:48px;height:48px;border-radius:50%;background:linear-gradient(135deg,#635bff,#4f46e5);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:1.1rem;flex-shrink:0;box-shadow:0 4px 16px rgba(99,91,255,.4);color:#fff">2</div><div><h3 style="font-size:1.1rem;margin-bottom:8px;font-family:'Outfit',sans-serif;font-weight:700;color:#f0f4ff">Upload your document</h3><p style="color:rgba(226,232,248,.5);font-size:.9rem;line-height:1.7">Drag and drop any PDF. The document loads instantly in the viewer so you can see every page.</p></div></div>
    <div class="card" style="display:flex;gap:20px;align-items:flex-start"><div style="width:48px;height:48px;border-radius:50%;background:linear-gradient(135deg,#635bff,#4f46e5);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:1.1rem;flex-shrink:0;box-shadow:0 4px 16px rgba(99,91,255,.4);color:#fff">3</div><div><h3 style="font-size:1.1rem;margin-bottom:8px;font-family:'Outfit',sans-serif;font-weight:700;color:#f0f4ff">Capture your signature</h3><p style="color:rgba(226,232,248,.5);font-size:.9rem;line-height:1.7">Open the camera, hold your signed paper up, and tap Capture. Or draw directly on screen with your finger or mouse.</p></div></div>
    <div class="card" style="display:flex;gap:20px;align-items:flex-start"><div style="width:48px;height:48px;border-radius:50%;background:linear-gradient(135deg,#635bff,#4f46e5);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:1.1rem;flex-shrink:0;box-shadow:0 4px 16px rgba(99,91,255,.4);color:#fff">4</div><div><h3 style="font-size:1.1rem;margin-bottom:8px;font-family:'Outfit',sans-serif;font-weight:700;color:#f0f4ff">Tell the AI where to sign</h3><p style="color:rgba(226,232,248,.5);font-size:.9rem;line-height:1.7">Type "bottom right", "above my printed name", or "next to the date". Click the button and watch the AI place it.</p></div></div>
    <div class="card" style="display:flex;gap:20px;align-items:flex-start"><div style="width:48px;height:48px;border-radius:50%;background:linear-gradient(135deg,#06d6a0,#059669);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:1.1rem;flex-shrink:0;box-shadow:0 4px 16px rgba(6,214,160,.4);color:#fff">5</div><div><h3 style="font-size:1.1rem;margin-bottom:8px;font-family:'Outfit',sans-serif;font-weight:700;color:#f0f4ff">Download your signed document</h3><p style="color:rgba(226,232,248,.5);font-size:.9rem;line-height:1.7">Click Sign & Download. Your professionally signed document is ready instantly. Done!</p></div></div>
  </div>
  <div style="text-align:center;margin-top:56px"><button class="btn btn-primary btn-lg" onclick="go('register')">Try it now — it's free →</button></div>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: CONTACT
═══════════════════════════════════════ -->
<div id="pg-contact" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-actions"><button class="back-btn" onclick="go('home')">Back to Home</button><button class="btn btn-ghost" onclick="go('login')">Log in</button><button class="btn btn-primary" onclick="go('register')">Get Started →</button></div></div></nav>
<div style="padding:80px 0"><div class="wrap" style="max-width:640px">
  <div class="section-lbl">Get in touch</div>
  <h2 class="section-h" style="margin-bottom:12px">Contact us</h2>
  <p style="color:rgba(226,232,248,.45);margin-bottom:40px">Have a question or issue? We respond within 24 hours.</p>
  <div class="card" style="position:relative;overflow:hidden">
    <div style="position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,#06d6a0,transparent)"></div>
    <div id="contact-ok" class="ok-msg hidden">✓ Message sent! We'll get back to you within 24 hours.</div>
    <div class="fgroup"><label>Your Name</label><input id="c-name" type="text" placeholder="Jane Smith"/></div>
    <div class="fgroup"><label>Email</label><input id="c-email" type="email" placeholder="you@example.com"/></div>
    <div class="fgroup"><label>Subject</label><select id="c-subject"><option>General question</option><option>Payment issue</option><option>Technical problem</option><option>Feature request</option><option>Other</option></select></div>
    <div class="fgroup"><label>Message</label><textarea id="c-msg" rows="5" placeholder="Tell us how we can help..."></textarea></div>
    <button class="btn btn-primary" onclick="sendContact()">Send Message →</button>
  </div>
  <div style="margin-top:32px;display:grid;grid-template-columns:1fr 1fr;gap:16px">
    <div class="card" style="text-align:center"><div style="font-size:1.8rem;margin-bottom:10px">📧</div><div style="font-size:.88rem;color:rgba(226,232,248,.5)">Email us at</div><div style="color:#a78bfa;font-size:.9rem;margin-top:4px">hello@signforge.app</div></div>
    <div class="card" style="text-align:center"><div style="font-size:1.8rem;margin-bottom:10px">⏰</div><div style="font-size:.88rem;color:rgba(226,232,248,.5)">Response time</div><div style="color:#06d6a0;font-size:.9rem;margin-top:4px">Within 24 hours</div></div>
  </div>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: TERMS
═══════════════════════════════════════ -->
<div id="pg-terms" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-actions"><button class="btn btn-ghost" onclick="go('home')">← Back</button></div></div></nav>
<div style="padding:64px 0"><div class="wrap" style="max-width:740px">
  <h1 style="font-size:2.2rem;margin-bottom:10px">Terms of Service</h1>
  <p style="color:rgba(226,232,248,.35);margin-bottom:40px;font-size:.88rem">Last updated: January 2025</p>
  <div style="display:flex;flex-direction:column;gap:28px">
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">1. Acceptance of Terms</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">By using SignForge, you agree to these terms. If you disagree, please do not use the service.</p></div>
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">2. Service Description</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">SignForge provides AI-powered document signing tools. We offer free trial (1 document) and paid subscription plans at $1.99/month (Starter) and $4.99/month (Pro).</p></div>
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">3. Payments & Refunds</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">Payments are processed securely by Stripe. Subscriptions auto-renew monthly. Cancel anytime. Refunds considered on a case-by-case basis within 7 days of purchase.</p></div>
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">4. Data & Privacy</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">Uploaded documents are automatically deleted within 24 hours. We never sell your data. Full details in our Privacy Policy.</p></div>
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">5. Limitation of Liability</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">SignForge is provided "as is". We are not liable for the legal validity of electronically signed documents in your jurisdiction. Always consult a legal professional for binding contracts.</p></div>
  </div>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: PRIVACY
═══════════════════════════════════════ -->
<div id="pg-privacy" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-actions"><button class="btn btn-ghost" onclick="go('home')">← Back</button></div></div></nav>
<div style="padding:64px 0"><div class="wrap" style="max-width:740px">
  <h1 style="font-size:2.2rem;margin-bottom:10px">Privacy Policy</h1>
  <p style="color:rgba(226,232,248,.35);margin-bottom:40px;font-size:.88rem">Last updated: January 2025</p>
  <div style="display:flex;flex-direction:column;gap:28px">
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">What we collect</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">Name, email address, and payment information (processed by Stripe — we never store your card details).</p></div>
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">Documents & Signatures</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">Documents you upload are temporarily processed and automatically deleted within 24 hours. Your signature is stored securely for reuse but can be deleted at any time.</p></div>
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">Your rights (GDPR)</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">You have the right to access, correct, or delete your data at any time. Contact us at hello@signforge.app.</p></div>
    <div><h2 style="font-size:1.1rem;color:#a78bfa;margin-bottom:10px;font-family:'Outfit',sans-serif;font-weight:700">No advertising</h2><p style="color:rgba(226,232,248,.55);line-height:1.8;font-size:.92rem">We do not sell your data to third parties or use it for advertising. Ever.</p></div>
  </div>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: LOGIN
═══════════════════════════════════════ -->
<div id="pg-login" class="page">
<div class="auth-wrap"><div class="auth-card">
  <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:24px">
    <span class="auth-logo" onclick="go('home')" style="margin-bottom:0">SignForge</span>
    <button class="back-btn" onclick="go('home')">Back</button>
  </div>
  <h1>Welcome back</h1>
  <p class="auth-sub">Sign in to your account to continue</p>
  <div id="login-err" class="err-msg hidden"></div>
  <div id="login-ok" class="ok-msg hidden"></div>
  <form id="login-form">
    <div class="fgroup"><label>Email</label><input id="l-email" type="email" placeholder="you@example.com" required/></div>
    <div class="fgroup"><label>Password</label><input id="l-pw" type="password" placeholder="••••••••" required/></div>
    <button type="submit" class="btn btn-primary btn-full" id="login-btn" style="margin-top:6px">Sign in</button>
  </form>
  <p class="auth-alt" style="margin-top:14px"><a onclick="go('forgot')">Forgot your password?</a></p>
  <p class="auth-alt"><a onclick="go('register')">Don't have an account? Sign up free →</a></p>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: REGISTER
═══════════════════════════════════════ -->
<div id="pg-register" class="page">
<div class="auth-wrap"><div class="auth-card">
  <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:24px">
    <span class="auth-logo" onclick="go('home')" style="margin-bottom:0">SignForge</span>
    <button class="back-btn" onclick="go('home')">Back</button>
  </div>
  <h1>Create account</h1>
  <p class="auth-sub">1 free document · No credit card needed</p>
  <div id="reg-err" class="err-msg hidden"></div>
  <div id="reg-ok" class="ok-msg hidden"></div>
  <form id="reg-form">
    <div class="fgroup"><label>Full Name</label><input id="r-name" type="text" placeholder="Jane Smith" required/></div>
    <div class="fgroup"><label>Email</label><input id="r-email" type="email" placeholder="you@example.com" required/></div>
    <div class="fgroup"><label>Password</label><input id="r-pw" type="password" placeholder="Minimum 8 characters" required/></div>
    <div class="fcheck"><input type="checkbox" id="r-terms" required/><label for="r-terms">I agree to the <a onclick="go('terms')" style="color:#a78bfa;cursor:pointer">Terms of Service</a> and <a onclick="go('privacy')" style="color:#a78bfa;cursor:pointer">Privacy Policy</a></label></div>
    <button type="submit" class="btn btn-primary btn-full" id="reg-btn">Create free account</button>
  </form>
  <p class="auth-alt"><a onclick="go('login')">Already have an account? Sign in →</a></p>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: FORGOT PASSWORD
═══════════════════════════════════════ -->
<div id="pg-forgot" class="page">
<div class="auth-wrap"><div class="auth-card">
  <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:24px">
    <span class="auth-logo" onclick="go('home')" style="margin-bottom:0">SignForge</span>
    <button class="back-btn" onclick="go('login')">Back to Login</button>
  </div>
  <h1>Reset password</h1>
  <p class="auth-sub">Enter your email and we'll send a reset link</p>
  <div id="fp-err" class="err-msg hidden"></div>
  <div id="fp-ok" class="ok-msg hidden"></div>
  <div class="fgroup"><label>Email</label><input id="fp-email" type="email" placeholder="you@example.com"/></div>
  <button class="btn btn-primary btn-full" onclick="doForgot()">Send reset link</button>
  <p class="auth-alt" style="margin-top:16px"><a onclick="go('login')">← Back to login</a></p>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: RESET PASSWORD
═══════════════════════════════════════ -->
<div id="pg-reset" class="page">
<div class="auth-wrap"><div class="auth-card">
  <span class="auth-logo" onclick="go('home')">SignForge</span>
  <h1>Set new password</h1>
  <div id="rp-err" class="err-msg hidden"></div>
  <div id="rp-ok" class="ok-msg hidden"></div>
  <div class="fgroup"><label>New Password</label><input id="rp-pw" type="password" placeholder="Minimum 8 characters"/></div>
  <div class="fgroup"><label>Confirm Password</label><input id="rp-pw2" type="password" placeholder="Repeat password"/></div>
  <button class="btn btn-primary btn-full" onclick="doReset()">Set new password</button>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: CHECKOUT
═══════════════════════════════════════ -->
<div id="pg-checkout" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-actions"><button class="back-btn" onclick="go('pricing')">Back to Pricing</button></div></div></nav>
<div style="padding:60px 0"><div class="wrap">
  <div style="text-align:center;margin-bottom:48px"><h1 style="font-size:2rem;color:#f0f4ff;margin-bottom:8px">Complete your subscription</h1><p style="color:rgba(226,232,248,.4)">Start with a 7-day free trial. Cancel anytime.</p></div>
  <div class="pay-layout">
    <div class="pay-summary">
      <div id="co-plan-badge" style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.4);text-transform:uppercase;letter-spacing:.12em;margin-bottom:12px">Pro Plan</div>
      <div style="display:flex;align-items:baseline;gap:4px;margin-bottom:6px"><span id="co-price" style="font-family:'Cormorant Garamond',serif;font-size:3rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text">$4.99</span><span style="color:rgba(226,232,248,.4)">/month</span></div>
      <p style="color:rgba(226,232,248,.4);font-size:.84rem;margin-bottom:24px;padding-bottom:20px;border-bottom:1px solid rgba(255,255,255,.07)">Billed monthly after 7-day trial</p>
      <div id="co-feats" style="display:flex;flex-direction:column;gap:10px">
        <div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> Unlimited documents</div>
        <div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> AI signature placement</div>
        <div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> Camera & draw signature</div>
        <div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> Document history</div>
        <div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> Priority support</div>
      </div>
      <div style="margin-top:24px;padding:14px;background:rgba(6,214,160,.08);border:1px solid rgba(6,214,160,.2);border-radius:10px;font-size:.84rem;color:#06d6a0">🎁 7-day free trial · No charge today</div>
    </div>
    <div class="pay-form-card">
      <div id="co-err" class="err-msg hidden"></div>
      <div id="co-ok" class="ok-msg hidden"></div>
      <div class="pay-methods">
        <button class="pay-btn" onclick="expressPay('apple')"><span style="font-size:1.3rem">🍎</span> Pay with Apple Pay</button>
        <button class="pay-btn" onclick="expressPay('google')"><span style="font-size:1.2rem;font-weight:900;color:#4285f4">G</span> Pay with Google Pay</button>
      </div>
      <div class="or-div">or pay by card</div>
      <form id="co-form" style="margin-top:16px">
        <div class="fgroup"><label>Name on card</label><input type="text" id="co-name" placeholder="Jane Smith" required/></div>
        <div class="fgroup"><label>Card Number</label><input type="text" id="co-num" placeholder="1234 5678 9012 3456" maxlength="19" required/></div>
        <div class="form-row">
          <div class="fgroup"><label>Expiry</label><input type="text" id="co-exp" placeholder="MM/YY" maxlength="5" required/></div>
          <div class="fgroup"><label>CVC</label><input type="text" id="co-cvc" placeholder="123" maxlength="4" required/></div>
        </div>
        <button type="submit" class="btn btn-primary btn-full" id="co-btn" style="margin-top:6px">Start 7-day free trial →</button>
      </form>
      <div class="secure-note">🔒 256-bit SSL encryption · Powered by Stripe</div>
      <p style="font-size:.76rem;color:rgba(226,232,248,.25);text-align:center;margin-top:10px">You won't be charged during your trial. Cancel anytime.</p>
    </div>
  </div>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: PAYMENT SUCCESS
═══════════════════════════════════════ -->
<div id="pg-payment-success" class="page">
<div class="auth-wrap"><div class="auth-card" style="text-align:center;max-width:500px">
  <div style="font-size:4rem;margin-bottom:18px">🎉</div>
  <h1 style="font-size:1.8rem;color:#06d6a0;margin-bottom:10px">Payment Successful!</h1>
  <p style="color:rgba(226,232,248,.5);margin-bottom:28px;line-height:1.7">Your subscription is now active. You have <strong style="color:#a78bfa" id="ps-plan-text">unlimited</strong> document signings per month.</p>
  <button class="btn btn-emerald btn-lg btn-full" onclick="go('dashboard')">Go to Dashboard →</button>
</div></div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: DASHBOARD
═══════════════════════════════════════ -->
<div id="pg-dashboard" class="page">
<div class="app-layout">
  <aside class="sidebar" id="sidebar">
    <span class="sidebar-logo" onclick="go('home')">SignForge</span>
    <nav class="sidebar-nav">
      <span class="slink active" id="sl-dashboard" onclick="go('dashboard')"><span class="ico">📊</span>Dashboard</span>
      <span class="slink" id="sl-sign" onclick="go('sign')"><span class="ico">✍️</span>Sign Document</span>
      <span class="slink" id="sl-billing" onclick="go('billing')"><span class="ico">💳</span>Billing</span>
      <span class="slink" id="sl-profile" onclick="go('profile')"><span class="ico">👤</span>Profile</span>
    </nav>
    <div class="sidebar-footer">
      <div class="sidebar-user" id="sb-user"><div class="uname" id="sb-name">Loading…</div><div class="uemail" id="sb-email"></div><span class="uplan" id="sb-plan">Free</span></div>
      <button class="s-logout" onclick="doLogout()">🚪 Sign out</button>
    </div>
  </aside>
  <main class="app-main">
    <div class="app-header"><h1>Dashboard</h1><span id="dash-plan" class="badge badge-purple">Free Plan</span></div>
    <div class="app-body">
      <div class="stat-grid">
        <div class="stat-card"><div class="stat-label">Documents Signed</div><div class="stat-val" id="s-total">–</div></div>
        <div class="stat-card"><div class="stat-label">Current Plan</div><div class="stat-val" id="s-plan">–</div></div>
        <div class="stat-card"><div class="stat-label">Remaining</div><div class="stat-val" id="s-rem">–</div></div>
      </div>
      <div id="dash-upgrade" class="upgrade-banner hidden">
        <div class="upgrade-banner-txt"><strong>🚀 Upgrade to sign unlimited documents</strong><span>Your free trial is used. Start at just $1.99/month.</span></div>
        <button class="btn btn-primary btn-sm" onclick="go('billing')">Upgrade now</button>
      </div>
      <div class="section-hdr"><h2>Recent Documents</h2><button class="btn btn-primary btn-sm" onclick="go('sign')">+ Sign New PDF</button></div>
      <div id="doc-list" class="doc-list"><div class="loading-txt">Loading…</div></div>
    </div>
  </main>
</div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: SIGN DOCUMENT
═══════════════════════════════════════ -->
<div id="pg-sign" class="page">
<div class="app-layout">
  <aside class="sidebar">
    <span class="sidebar-logo" onclick="go('home')">SignForge</span>
    <nav class="sidebar-nav">
      <span class="slink" onclick="go('dashboard')"><span class="ico">📊</span>Dashboard</span>
      <span class="slink active" onclick="go('sign')"><span class="ico">✍️</span>Sign Document</span>
      <span class="slink" onclick="go('billing')"><span class="ico">💳</span>Billing</span>
      <span class="slink" onclick="go('profile')"><span class="ico">👤</span>Profile</span>
    </nav>
    <div class="sidebar-footer"><button class="s-logout" onclick="doLogout()">🚪 Sign out</button></div>
  </aside>
  <main class="app-main">
    <div class="app-header"><h1>Sign a Document</h1></div>
    <div class="app-body">

      <!-- Trial used warning -->
      <div id="trial-wall" class="hidden">
        <div style="text-align:center;padding:60px 20px">
          <div style="font-size:3rem;margin-bottom:18px">🔒</div>
          <h2 style="color:#f0f4ff;margin-bottom:12px">Your free trial has been used</h2>
          <p style="color:rgba(226,232,248,.5);margin-bottom:28px;font-size:1rem">Upgrade to keep signing documents. Start at just $1.99/month.</p>
          <button class="btn btn-primary btn-lg" onclick="go('billing')">View plans & upgrade →</button>
        </div>
      </div>

      <div id="sign-form">
        <!-- STEP 1: Upload -->
        <div class="sign-step">
          <div class="step-hdr"><div class="step-num">1</div><h2>Upload your document</h2></div>
          <label class="drop-zone" id="drop-z">
            <input type="file" id="pdf-inp" accept="application/pdf,image/*" hidden/>
            <div class="drop-icon">📄</div>
            <p>Drop your PDF here or <strong>click to browse</strong></p>
            <p style="font-size:.78rem;color:rgba(226,232,248,.25)">PDF · JPG · PNG · Max 20MB</p>
          </label>
          <div id="pdf-status" class="hidden" style="margin-top:12px"></div>
        </div>

        <!-- STEP 2: Signature -->
        <div class="sign-step">
          <div class="step-hdr"><div class="step-num">2</div><h2>Add your signature</h2></div>
          <div class="tab-bar">
            <button class="tab active" onclick="switchTab('draw',this)">✏️ Draw</button>
            <button class="tab" onclick="switchTab('camera',this)">📷 Camera</button>
            <button class="tab" onclick="switchTab('upload',this)">🖼️ Upload Image</button>
          </div>

          <!-- Draw -->
          <div id="tab-draw" class="tab-panel">
            <canvas id="sig-canvas" width="500" height="160"></canvas>
            <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:10px;margin-top:10px">
              <div class="swatches">
                <div class="swatch active" style="background:#e2e8f8" data-c="#e2e8f8" onclick="setClr(this)"></div>
                <div class="swatch" style="background:#635bff" data-c="#635bff" onclick="setClr(this)"></div>
                <div class="swatch" style="background:#06d6a0" data-c="#06d6a0" onclick="setClr(this)"></div>
                <div class="swatch" style="background:#ff4f7b" data-c="#ff4f7b" onclick="setClr(this)"></div>
                <div class="swatch" style="background:#000" data-c="#111" onclick="setClr(this)"></div>
              </div>
              <div style="display:flex;gap:8px">
                <button class="btn btn-ghost btn-sm" onclick="clearSig()">Clear</button>
                <button class="btn btn-primary btn-sm" onclick="useDraw()">Use this signature</button>
              </div>
            </div>
          </div>

          <!-- Camera -->
          <div id="tab-camera" class="tab-panel hidden">
            <div class="cam-wrap"><video id="cam-vid" autoplay playsinline></video></div>
            <canvas id="cam-cvs" hidden></canvas>
            <img id="cam-prev" style="max-width:100%;max-height:120px;border-radius:8px;margin-top:8px;display:none" alt="Captured sig"/>
            <div style="display:flex;gap:10px;flex-wrap:wrap;margin-top:12px">
              <button class="btn btn-primary btn-sm" id="cam-open-btn" onclick="openCam()">📷 Open Camera</button>
              <button class="btn btn-emerald btn-sm hidden" id="cam-cap-btn" onclick="captureCam()">📸 Capture</button>
              <button class="btn btn-ghost btn-sm hidden" id="cam-ret-btn" onclick="retakeCam()">↩ Retake</button>
              <button class="btn btn-primary btn-sm hidden" id="cam-use-btn" onclick="useCam()">✓ Use this</button>
            </div>
            <p style="font-size:.78rem;color:rgba(226,232,248,.3);margin-top:8px">Tip: Hold your signed paper up to the camera for the cleanest result.</p>
          </div>

          <!-- Upload -->
          <div id="tab-upload" class="tab-panel hidden">
            <label class="drop-zone" style="padding:24px">
              <input type="file" id="sig-file-inp" accept="image/*" hidden onchange="loadSigFile(event)"/>
              <div class="drop-icon">🖼️</div>
              <p>Click to upload a signature image</p>
            </label>
            <img id="sig-file-prev" style="max-height:120px;border-radius:8px;margin-top:10px;display:none" alt="Signature"/>
          </div>

          <div id="sig-ok" class="sig-ready-banner hidden">✅ Signature captured — scroll down to place it</div>
        </div>

        <!-- STEP 3: AI Placement -->
        <div class="sign-step">
          <div class="step-hdr"><div class="step-num">3</div><h2>Tell the AI where to place your signature</h2></div>

          <!-- AI instruction box - prominent -->
          <div style="background:linear-gradient(135deg,rgba(99,91,255,.12),rgba(6,214,160,.06));border:1.5px solid rgba(99,91,255,.3);border-radius:14px;padding:20px;margin-bottom:18px">
            <div style="font-size:.78rem;font-weight:700;color:#a78bfa;text-transform:uppercase;letter-spacing:.1em;margin-bottom:10px">🤖 Tell the AI where to sign</div>
            <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
              <input id="ai-instruction" type="text" placeholder='Type where e.g. "bottom right", "above my name", "next to the date"…' style="flex:1;min-width:200px;padding:13px 16px;border:1.5px solid rgba(99,91,255,.3);border-radius:10px;background:rgba(7,9,15,.6);color:#e2e8f8;font-family:'Outfit',sans-serif;font-size:.95rem"/>
              <button class="btn btn-primary" onclick="doAIPlace()" style="white-space:nowrap">🤖 Place Signature</button>
            </div>
            <div style="display:flex;gap:7px;flex-wrap:wrap;margin-top:12px">
              <span class="ai-chip" onclick="setAI('bottom right corner')">📍 Bottom right</span>
              <span class="ai-chip" onclick="setAI('bottom left corner')">📍 Bottom left</span>
              <span class="ai-chip" onclick="setAI('center of the page')">📍 Centre</span>
              <span class="ai-chip" onclick="setAI('above my printed name')">📍 Above my name</span>
              <span class="ai-chip" onclick="setAI('next to the date field')">📍 Next to date</span>
              <span class="ai-chip" onclick="setAI('top right corner')">📍 Top right</span>
              <span class="ai-chip" onclick="setAI('below the signature line')">📍 Signature line</span>
            </div>
            <p style="font-size:.78rem;color:rgba(226,232,248,.3);margin-top:10px">💡 Tip: After AI places it, you can still <strong style="color:rgba(226,232,248,.5)">drag</strong> it anywhere and <strong style="color:rgba(226,232,248,.5)">resize</strong> using the corner handle</p>
          </div>

          <!-- AI thinking animation -->
          <div id="ai-thinking" class="hidden ai-processing" style="margin-bottom:16px">
            <div class="ai-spinner"></div>
            <div class="ai-label">AI is analysing your document…</div>
            <p style="font-size:.84rem;color:rgba(226,232,248,.35)">Finding the best placement position</p>
          </div>

          <!-- AI result banner -->
          <div id="ai-result" class="hidden" style="background:rgba(6,214,160,.08);border:1px solid rgba(6,214,160,.25);border-radius:10px;padding:12px 18px;margin-bottom:14px;font-size:.88rem;color:#06d6a0;display:flex;align-items:center;gap:10px">
            <span style="font-size:1.2rem">✅</span>
            <span id="ai-result-txt">Signature placed bottom right</span>
            <span style="margin-left:auto;font-size:.78rem;color:rgba(6,214,160,.5)">Drag to reposition</span>
          </div>

          <!-- PDF Viewer -->
          <div style="background:rgba(255,255,255,.02);border:1px solid rgba(255,255,255,.07);border-radius:12px;padding:14px;margin-bottom:14px">
            <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:10px;flex-wrap:wrap;gap:8px">
              <span style="font-size:.78rem;color:rgba(226,232,248,.35);font-weight:600">📄 DOCUMENT PREVIEW — drag the signature overlay to reposition</span>
              <div style="display:flex;align-items:center;gap:8px">
                <button class="btn btn-ghost btn-sm" id="prev-btn" onclick="changePg(-1)" disabled>◀ Prev</button>
                <span id="pg-lbl" style="font-size:.85rem;color:rgba(226,232,248,.4)">Page 1/1</span>
                <button class="btn btn-ghost btn-sm" id="next-btn" onclick="changePg(1)" disabled>Next ▶</button>
              </div>
            </div>
            <div class="pdf-viewer-wrap" style="width:100%;min-height:300px;border-radius:8px;overflow:hidden">
              <canvas id="pdf-canvas"></canvas>
              <div id="sig-overlay" class="sig-overlay hidden">
                <img id="sig-ov-img" draggable="false" alt="sig"/>
                <div class="resize-handle" id="rz-handle"></div>
              </div>
            </div>
          </div>

          <!-- Download button - big and clear -->
          <div style="background:rgba(6,214,160,.06);border:1px solid rgba(6,214,160,.2);border-radius:14px;padding:20px;display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:16px">
            <div>
              <div style="font-weight:700;color:#f0f4ff;margin-bottom:4px">Ready to download?</div>
              <div style="font-size:.84rem;color:rgba(226,232,248,.4)">Your signature will be permanently embedded into the PDF</div>
            </div>
            <div style="display:flex;align-items:center;gap:14px;flex-wrap:wrap">
              <span id="sign-lbl" class="status-lbl"></span>
              <button class="btn btn-emerald btn-lg" id="dl-btn" onclick="signAndDownload()" disabled>⬇️ Sign & Download PDF</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: BILLING
═══════════════════════════════════════ -->
<div id="pg-billing" class="page">
<div class="app-layout">
  <aside class="sidebar">
    <span class="sidebar-logo" onclick="go('home')">SignForge</span>
    <nav class="sidebar-nav">
      <span class="slink" onclick="go('dashboard')"><span class="ico">📊</span>Dashboard</span>
      <span class="slink" onclick="go('sign')"><span class="ico">✍️</span>Sign Document</span>
      <span class="slink active" onclick="go('billing')"><span class="ico">💳</span>Billing</span>
      <span class="slink" onclick="go('profile')"><span class="ico">👤</span>Profile</span>
    </nav>
    <div class="sidebar-footer"><button class="s-logout" onclick="doLogout()">🚪 Sign out</button></div>
  </aside>
  <main class="app-main">
    <div class="app-header"><h1>Billing & Subscription</h1></div>
    <div class="app-body" id="billing-body"><div class="loading-txt">Loading…</div></div>
  </main>
</div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: PROFILE
═══════════════════════════════════════ -->
<div id="pg-profile" class="page">
<div class="app-layout">
  <aside class="sidebar">
    <span class="sidebar-logo" onclick="go('home')">SignForge</span>
    <nav class="sidebar-nav">
      <span class="slink" onclick="go('dashboard')"><span class="ico">📊</span>Dashboard</span>
      <span class="slink" onclick="go('sign')"><span class="ico">✍️</span>Sign Document</span>
      <span class="slink" onclick="go('billing')"><span class="ico">💳</span>Billing</span>
      <span class="slink active" onclick="go('profile')"><span class="ico">👤</span>Profile</span>
    </nav>
    <div class="sidebar-footer"><button class="s-logout" onclick="doLogout()">🚪 Sign out</button></div>
  </aside>
  <main class="app-main">
    <div class="app-header"><h1>Profile Settings</h1></div>
    <div class="app-body">
      <div class="card" style="max-width:540px;position:relative;overflow:hidden">
        <div style="position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,#06d6a0,transparent)"></div>
        <div id="prof-msg" class="hidden" style="margin-bottom:16px"></div>
        <form id="prof-form">
          <div class="fgroup"><label>Full Name</label><input id="p-name" type="text" required/></div>
          <div class="fgroup"><label>Email</label><input id="p-email" type="email" disabled/></div>
          <hr style="border:none;border-top:1px solid rgba(255,255,255,.07);margin:20px 0"/>
          <div class="fgroup"><label>New Password <span style="opacity:.4;font-size:.78rem">(leave blank to keep current)</span></label><input id="p-pw" type="password" placeholder="Minimum 8 characters"/></div>
          <button type="submit" class="btn btn-primary" style="margin-top:6px">Save Changes</button>
        </form>
        <hr style="border:none;border-top:1px solid rgba(255,255,255,.07);margin:24px 0"/>
        <h3 style="font-size:1rem;color:#f0f4ff;margin-bottom:14px;font-family:'Outfit',sans-serif;font-weight:700">Saved Signature</h3>
        <div id="saved-sig-wrap">
          <p style="font-size:.86rem;color:rgba(226,232,248,.35);margin-bottom:12px">Your signature is saved for quick use when signing documents.</p>
          <img id="saved-sig-img" style="max-height:80px;border:1px solid rgba(255,255,255,.1);border-radius:8px;padding:8px;display:none" alt="Saved signature"/>
          <p id="no-sig-txt" style="font-size:.84rem;color:rgba(226,232,248,.3)">No signature saved yet. Sign a document to save yours.</p>
        </div>
      </div>
    </div>
  </main>
</div>
</div>

<!-- ═══════════════════════════════════════
     PAGE: HOW IT ALL WORKS (Accounts, Storage, Stripe)
═══════════════════════════════════════ -->
<div id="pg-info" class="page">
<nav class="top"><div class="wrap nav-inner"><span class="logo" onclick="go('home')">SignForge</span><div class="nav-actions"><button class="back-btn" onclick="go('home')">Back to Home</button></div></div></nav>
<div style="padding:60px 0"><div class="wrap" style="max-width:820px">
  <div class="section-lbl">Under the hood</div>
  <h1 style="font-size:clamp(2rem,4vw,3rem);margin-bottom:12px">How everything works</h1>
  <p style="color:rgba(226,232,248,.45);margin-bottom:52px;font-size:1rem">Where user data is stored, how payments reach you, and what each account does.</p>

  <!-- USER ACCOUNTS + STORAGE -->
  <div class="card" style="margin-bottom:22px;position:relative;overflow:hidden">
    <div style="position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,transparent)"></div>
    <div style="display:flex;align-items:center;gap:14px;margin-bottom:20px">
      <div style="width:44px;height:44px;border-radius:12px;background:rgba(99,91,255,.2);border:1px solid rgba(99,91,255,.3);display:flex;align-items:center;justify-content:center;font-size:1.4rem;flex-shrink:0">👤</div>
      <h2 style="font-size:1.25rem;color:#f0f4ff">User Accounts & Data Storage</h2>
    </div>
    <div style="display:flex;flex-direction:column;gap:14px">
      <div style="background:rgba(99,91,255,.07);border:1px solid rgba(99,91,255,.2);border-radius:12px;padding:18px">
        <div style="font-weight:700;color:#a78bfa;margin-bottom:8px">Right now (development mode)</div>
        <p style="color:rgba(226,232,248,.6);font-size:.9rem;line-height:1.75">Accounts are stored in <strong style="color:#e2e8f8">server memory</strong> — this means they work perfectly while the server is running, but they reset when you restart the server. This is normal for development and testing. Your first 100 test users will be fine.</p>
      </div>
      <div style="background:rgba(6,214,160,.06);border:1px solid rgba(6,214,160,.2);border-radius:12px;padding:18px">
        <div style="font-weight:700;color:#06d6a0;margin-bottom:8px">When you go live (production)</div>
        <p style="color:rgba(226,232,248,.6);font-size:.9rem;line-height:1.75">Accounts will be stored in a <strong style="color:#e2e8f8">PostgreSQL database</strong> (like a permanent spreadsheet that never resets). The easiest free option is <strong style="color:#06d6a0">Supabase.com</strong> — free up to 50,000 users. I can upgrade the code for you when you're ready.</p>
      </div>
    </div>

    <div style="margin-top:20px;display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:12px">
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:10px;padding:16px">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px">What gets stored</div>
        <div style="font-size:.88rem;color:rgba(226,232,248,.65);line-height:1.8">✓ Name & email<br/>✓ Hashed password<br/>✓ Plan (free/starter/pro)<br/>✓ Trial used or not<br/>✓ Document history<br/>✓ Saved signature</div>
      </div>
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:10px;padding:16px">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px">What is NOT stored</div>
        <div style="font-size:.88rem;color:rgba(226,232,248,.65);line-height:1.8">✗ Actual password (only hash)<br/>✗ Card details (Stripe handles)<br/>✗ PDF documents (deleted in 24h)<br/>✗ Any tracking data<br/>✗ IP addresses</div>
      </div>
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:10px;padding:16px">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px">Password security</div>
        <div style="font-size:.88rem;color:rgba(226,232,248,.65);line-height:1.8">Passwords are <strong style="color:#a78bfa">hashed</strong> with bcrypt before storage. This means even if someone stole the database, they could never see real passwords. It's industry standard security used by all major apps.</div>
      </div>
    </div>
  </div>

  <!-- STRIPE PAYMENTS -->
  <div class="card" style="margin-bottom:22px;position:relative;overflow:hidden">
    <div style="position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#06d6a0,transparent)"></div>
    <div style="display:flex;align-items:center;gap:14px;margin-bottom:20px">
      <div style="width:44px;height:44px;border-radius:12px;background:rgba(6,214,160,.15);border:1px solid rgba(6,214,160,.3);display:flex;align-items:center;justify-content:center;font-size:1.4rem;flex-shrink:0">💳</div>
      <h2 style="font-size:1.25rem;color:#f0f4ff">How Stripe Payments Work</h2>
    </div>

    <!-- Money flow diagram -->
    <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px;padding:20px;margin-bottom:20px">
      <div style="font-size:.8rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:16px">The money flow</div>
      <div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;justify-content:center">
        <div style="text-align:center;padding:12px 16px;background:rgba(99,91,255,.12);border:1px solid rgba(99,91,255,.25);border-radius:10px;min-width:110px">
          <div style="font-size:1.4rem;margin-bottom:6px">👤</div>
          <div style="font-size:.78rem;font-weight:700;color:#a78bfa">Customer</div>
          <div style="font-size:.72rem;color:rgba(226,232,248,.4);margin-top:2px">Pays $4.99</div>
        </div>
        <div style="color:rgba(226,232,248,.3);font-size:1.2rem">→</div>
        <div style="text-align:center;padding:12px 16px;background:rgba(99,91,255,.08);border:1px solid rgba(99,91,255,.2);border-radius:10px;min-width:110px">
          <div style="font-size:1.4rem;margin-bottom:6px">🔒</div>
          <div style="font-size:.78rem;font-weight:700;color:#a78bfa">Stripe</div>
          <div style="font-size:.72rem;color:rgba(226,232,248,.4);margin-top:2px">Takes $0.44 fee</div>
        </div>
        <div style="color:rgba(226,232,248,.3);font-size:1.2rem">→</div>
        <div style="text-align:center;padding:12px 16px;background:rgba(6,214,160,.1);border:1px solid rgba(6,214,160,.25);border-radius:10px;min-width:110px">
          <div style="font-size:1.4rem;margin-bottom:6px">💼</div>
          <div style="font-size:.78rem;font-weight:700;color:#06d6a0">Your Stripe</div>
          <div style="font-size:.72rem;color:rgba(226,232,248,.4);margin-top:2px">Holds $4.55</div>
        </div>
        <div style="color:rgba(226,232,248,.3);font-size:1.2rem">→</div>
        <div style="text-align:center;padding:12px 16px;background:rgba(6,214,160,.15);border:1px solid rgba(6,214,160,.3);border-radius:10px;min-width:110px">
          <div style="font-size:1.4rem;margin-bottom:6px">🏦</div>
          <div style="font-size:.78rem;font-weight:700;color:#06d6a0">Your Bank</div>
          <div style="font-size:.72rem;color:rgba(226,232,248,.4);margin-top:2px">Every 2 days</div>
        </div>
      </div>
    </div>

    <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:14px;margin-bottom:20px">
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:10px;padding:16px">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px">Stripe's fee</div>
        <div style="font-size:.9rem;color:rgba(226,232,248,.7);line-height:1.8">
          <span style="color:#ffb347">2.9% + $0.30</span> per transaction<br/>
          On $1.99 → you keep <strong style="color:#06d6a0">$1.63</strong><br/>
          On $4.99 → you keep <strong style="color:#06d6a0">$4.55</strong>
        </div>
      </div>
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:10px;padding:16px">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px">When you get paid</div>
        <div style="font-size:.9rem;color:rgba(226,232,248,.7);line-height:1.8">
          Stripe pays out to your bank account <strong style="color:#06d6a0">every 2 business days</strong> automatically. You can also do instant payouts for a small fee.
        </div>
      </div>
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:10px;padding:16px">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px">You never touch cards</div>
        <div style="font-size:.9rem;color:rgba(226,232,248,.7);line-height:1.8">
          Stripe handles all card security. You never see card numbers. This is why major companies use Stripe — it's the safest option.
        </div>
      </div>
    </div>

    <div style="background:rgba(6,214,160,.06);border:1px solid rgba(6,214,160,.2);border-radius:12px;padding:20px">
      <div style="font-weight:700;color:#06d6a0;margin-bottom:14px;font-size:.95rem">📋 Step-by-step: Set up Stripe to receive money</div>
      <div style="display:flex;flex-direction:column;gap:10px">
        <div style="display:flex;gap:12px;align-items:flex-start"><div style="width:24px;height:24px;border-radius:50%;background:#06d6a0;color:#07090f;display:flex;align-items:center;justify-content:center;font-size:.72rem;font-weight:700;flex-shrink:0;margin-top:1px">1</div><div style="font-size:.88rem;color:rgba(226,232,248,.7)">Go to <strong style="color:#06d6a0">stripe.com</strong> and create a free account with your email</div></div>
        <div style="display:flex;gap:12px;align-items:flex-start"><div style="width:24px;height:24px;border-radius:50%;background:#06d6a0;color:#07090f;display:flex;align-items:center;justify-content:center;font-size:.72rem;font-weight:700;flex-shrink:0;margin-top:1px">2</div><div style="font-size:.88rem;color:rgba(226,232,248,.7)">In the Stripe dashboard go to <strong style="color:#e2e8f8">Settings → Bank accounts</strong> → add your bank account (sort code + account number)</div></div>
        <div style="display:flex;gap:12px;align-items:flex-start"><div style="width:24px;height:24px;border-radius:50%;background:#06d6a0;color:#07090f;display:flex;align-items:center;justify-content:center;font-size:.72rem;font-weight:700;flex-shrink:0;margin-top:1px">3</div><div style="font-size:.88rem;color:rgba(226,232,248,.7)">Go to <strong style="color:#e2e8f8">Developers → API Keys</strong> → copy your Secret Key and Publishable Key</div></div>
        <div style="display:flex;gap:12px;align-items:flex-start"><div style="width:24px;height:24px;border-radius:50%;background:#06d6a0;color:#07090f;display:flex;align-items:center;justify-content:center;font-size:.72rem;font-weight:700;flex-shrink:0;margin-top:1px">4</div><div style="font-size:.88rem;color:rgba(226,232,248,.7)">Open your <strong style="color:#e2e8f8">.env file</strong> in VS Code and paste in both keys where it says REPLACE_ME</div></div>
        <div style="display:flex;gap:12px;align-items:flex-start"><div style="width:24px;height:24px;border-radius:50%;background:#06d6a0;color:#07090f;display:flex;align-items:center;justify-content:center;font-size:.72rem;font-weight:700;flex-shrink:0;margin-top:1px">5</div><div style="font-size:.88rem;color:rgba(226,232,248,.7)">Go to <strong style="color:#e2e8f8">Products</strong> in Stripe → create "Starter $1.99/month" and "Pro $4.99/month" → copy the Price IDs into .env</div></div>
        <div style="display:flex;gap:12px;align-items:flex-start"><div style="width:24px;height:24px;border-radius:50%;background:#06d6a0;color:#07090f;display:flex;align-items:center;justify-content:center;font-size:.72rem;font-weight:700;flex-shrink:0;margin-top:1px">6</div><div style="font-size:.88rem;color:rgba(226,232,248,.7)">Restart the server (<strong style="color:#e2e8f8">Ctrl+C then npm start</strong>) — payments now work and money goes to your bank! 🎉</div></div>
      </div>
    </div>
  </div>

  <!-- ACCOUNTS NEEDED -->
  <div class="card" style="margin-bottom:22px;position:relative;overflow:hidden">
    <div style="position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#a78bfa,transparent)"></div>
    <div style="display:flex;align-items:center;gap:14px;margin-bottom:20px">
      <div style="width:44px;height:44px;border-radius:12px;background:rgba(167,139,250,.15);border:1px solid rgba(167,139,250,.3);display:flex;align-items:center;justify-content:center;font-size:1.4rem;flex-shrink:0">🗂️</div>
      <h2 style="font-size:1.25rem;color:#f0f4ff">Accounts You Need to Create</h2>
    </div>
    <div style="display:flex;flex-direction:column;gap:12px">
      <div style="display:flex;gap:16px;align-items:center;padding:16px;background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px">
        <div style="font-size:1.8rem;flex-shrink:0">💳</div>
        <div style="flex:1"><div style="font-weight:700;color:#f0f4ff;margin-bottom:3px">Stripe — stripe.com</div><div style="font-size:.86rem;color:rgba(226,232,248,.5)">To receive payments. <strong style="color:#06d6a0">FREE</strong> to create. They only take their fee when you get paid.</div></div>
        <span style="padding:4px 12px;background:rgba(6,214,160,.12);color:#06d6a0;border-radius:50px;font-size:.74rem;font-weight:700;white-space:nowrap">Required</span>
      </div>
      <div style="display:flex;gap:16px;align-items:center;padding:16px;background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px">
        <div style="font-size:1.8rem;flex-shrink:0">📧</div>
        <div style="flex:1"><div style="font-weight:700;color:#f0f4ff;margin-bottom:3px">Resend — resend.com</div><div style="font-size:.86rem;color:rgba(226,232,248,.5)">To send emails (welcome, payment confirmation etc). <strong style="color:#06d6a0">FREE up to 3,000 emails/month</strong>.</div></div>
        <span style="padding:4px 12px;background:rgba(6,214,160,.12);color:#06d6a0;border-radius:50px;font-size:.74rem;font-weight:700;white-space:nowrap">Required</span>
      </div>
      <div style="display:flex;gap:16px;align-items:center;padding:16px;background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px">
        <div style="font-size:1.8rem;flex-shrink:0">🌐</div>
        <div style="flex:1"><div style="font-weight:700;color:#f0f4ff;margin-bottom:3px">Render — render.com</div><div style="font-size:.86rem;color:rgba(226,232,248,.5)">To host the website online. <strong style="color:#06d6a0">FREE tier</strong> available. Paid from $7/month for always-on.</div></div>
        <span style="padding:4px 12px;background:rgba(99,91,255,.15);color:#a78bfa;border-radius:50px;font-size:.74rem;font-weight:700;white-space:nowrap">For launch</span>
      </div>
      <div style="display:flex;gap:16px;align-items:center;padding:16px;background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px">
        <div style="font-size:1.8rem;flex-shrink:0">🔗</div>
        <div style="flex:1"><div style="font-weight:700;color:#f0f4ff;margin-bottom:3px">Namecheap — namecheap.com</div><div style="font-size:.86rem;color:rgba(226,232,248,.5)">To buy your domain name (e.g. signforge.com). <strong style="color:#ffb347">~$12/year</strong>.</div></div>
        <span style="padding:4px 12px;background:rgba(99,91,255,.15);color:#a78bfa;border-radius:50px;font-size:.74rem;font-weight:700;white-space:nowrap">For launch</span>
      </div>
      <div style="display:flex;gap:16px;align-items:center;padding:16px;background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px">
        <div style="font-size:1.8rem;flex-shrink:0">🗄️</div>
        <div style="flex:1"><div style="font-weight:700;color:#f0f4ff;margin-bottom:3px">Supabase — supabase.com</div><div style="font-size:.86rem;color:rgba(226,232,248,.5)">Permanent database for user accounts. <strong style="color:#06d6a0">FREE up to 50,000 users</strong>. Only needed when you go live.</div></div>
        <span style="padding:4px 12px;background:rgba(255,179,71,.1);color:#ffb347;border-radius:50px;font-size:.74rem;font-weight:700;white-space:nowrap">Later</span>
      </div>
    </div>
  </div>

  <!-- REVENUE CALCULATOR -->
  <div class="card" style="position:relative;overflow:hidden">
    <div style="position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,transparent,#635bff,#06d6a0,transparent)"></div>
    <div style="display:flex;align-items:center;gap:14px;margin-bottom:20px">
      <div style="width:44px;height:44px;border-radius:12px;background:rgba(99,91,255,.15);border:1px solid rgba(99,91,255,.25);display:flex;align-items:center;justify-content:center;font-size:1.4rem;flex-shrink:0">📈</div>
      <h2 style="font-size:1.25rem;color:#f0f4ff">Revenue Calculator</h2>
    </div>
    <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:14px">
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px;padding:18px;text-align:center">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px">10 Pro users</div>
        <div style="font-family:'Cormorant Garamond',serif;font-size:2rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text">$45/mo</div>
        <div style="font-size:.76rem;color:rgba(226,232,248,.3);margin-top:4px">after Stripe fees</div>
      </div>
      <div style="background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px;padding:18px;text-align:center">
        <div style="font-size:.76rem;font-weight:700;color:rgba(226,232,248,.35);text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px">100 Pro users</div>
        <div style="font-family:'Cormorant Garamond',serif;font-size:2rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text">$455/mo</div>
        <div style="font-size:.76rem;color:rgba(226,232,248,.3);margin-top:4px">after Stripe fees</div>
      </div>
      <div style="background:rgba(99,91,255,.1);border:1px solid rgba(99,91,255,.25);border-radius:12px;padding:18px;text-align:center">
        <div style="font-size:.76rem;font-weight:700;color:#a78bfa;text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px">500 Pro users</div>
        <div style="font-family:'Cormorant Garamond',serif;font-size:2rem;font-weight:700;background:linear-gradient(135deg,#a78bfa,#06d6a0);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text">$2,275/mo</div>
        <div style="font-size:.76rem;color:rgba(226,232,248,.3);margin-top:4px">after Stripe fees</div>
      </div>
      <div style="background:rgba(6,214,160,.08);border:1px solid rgba(6,214,160,.25);border-radius:12px;padding:18px;text-align:center">
        <div style="font-size:.76rem;font-weight:700;color:#06d6a0;text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px">1000 Pro users</div>
        <div style="font-family:'Cormorant Garamond',serif;font-size:2rem;font-weight:700;background:linear-gradient(135deg,#06d6a0,#a78bfa);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text">$4,550/mo</div>
        <div style="font-size:.76rem;color:rgba(226,232,248,.3);margin-top:4px">after Stripe fees</div>
      </div>
    </div>
  </div>

  <div style="text-align:center;margin-top:40px;display:flex;gap:12px;justify-content:center;flex-wrap:wrap">
    <button class="btn btn-primary btn-lg" onclick="go('register')">Create your account →</button>
    <button class="btn btn-ghost btn-lg" onclick="go('home')">Back to home</button>
  </div>
</div></div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/pdf-lib@1.17.1/dist/pdf-lib.min.js"></script>
<script>
pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';

// ═══════════════════════════════════════
//  STATE
// ═══════════════════════════════════════
let user=null, pdfDoc=null, curPage=1, sigDataURL=null, camStream=null;
let drawClr='#e2e8f8', isDrawing=false, lx=0, ly=0, checkoutPlan='pro';

// ═══════════════════════════════════════
//  ROUTING
// ═══════════════════════════════════════
const AUTH_PAGES = ['dashboard','sign','billing','profile'];
const PAGE_MAP = {
  home:'pg-home', features:'pg-features', pricing:'pg-pricing', how:'pg-how',
  contact:'pg-contact', terms:'pg-terms', privacy:'pg-privacy',
  login:'pg-login', register:'pg-register', forgot:'pg-forgot', reset:'pg-reset',
  checkout:'pg-checkout', 'payment-success':'pg-payment-success',
  dashboard:'pg-dashboard', sign:'pg-sign', billing:'pg-billing', profile:'pg-profile',
  info:'pg-info'
};

function go(page, data) {
  if (AUTH_PAGES.includes(page) && !user) { go('login'); return; }
  Object.values(PAGE_MAP).forEach(id => { const el=document.getElementById(id); if(el) el.classList.remove('active'); });
  const pid = PAGE_MAP[page];
  if (!pid) return;
  const el = document.getElementById(pid);
  if (el) el.classList.add('active');
  window.scrollTo(0,0);
  stopCam();
  history.pushState({ page, data }, '', '/'+page);
  document.title = pageTitles[page] || 'SignForge';

  // Page-specific init
  if (page==='dashboard') initDashboard();
  if (page==='billing') initBilling();
  if (page==='profile') initProfile();
  if (page==='sign') initSign(data);
  if (page==='checkout') initCheckout(data);
  if (page==='payment-success') initPaySuccess();
  updateSidebarUser();
}

const pageTitles = {
  home:'SignForge – AI Document Signing', login:'Sign In – SignForge', register:'Create Account – SignForge',
  dashboard:'Dashboard – SignForge', sign:'Sign Document – SignForge', billing:'Billing – SignForge',
  pricing:'Pricing – SignForge', features:'Features – SignForge', profile:'Profile – SignForge',
  checkout:'Checkout – SignForge', contact:'Contact – SignForge'
};

window.addEventListener('popstate', e => { if (e.state?.page) go(e.state.page, e.state.data); });

// ═══════════════════════════════════════
//  API
// ═══════════════════════════════════════
async function api(path, method='GET', body=null) {
  const opts = { method, headers:{'Content-Type':'application/json'}, credentials:'include' };
  if (body) opts.body = JSON.stringify(body);
  const res = await fetch('/api'+path, opts);
  return res;
}

// ═══════════════════════════════════════
//  AUTH
// ═══════════════════════════════════════
async function checkSession() {
  try { const r=await api('/auth/me'); if(r.ok) user=await r.json(); } catch(_) {}
}

document.getElementById('login-form').addEventListener('submit', async e => {
  e.preventDefault();
  const btn=document.getElementById('login-btn'), err=document.getElementById('login-err'), ok=document.getElementById('login-ok');
  err.classList.add('hidden'); btn.disabled=true; btn.textContent='Signing in…';
  try {
    const r = await api('/auth/login','POST',{ email:document.getElementById('l-email').value.trim(), password:document.getElementById('l-pw').value });
    const d = await r.json();
    if (!r.ok) throw new Error(d.error||'Login failed');
    user=d.user; ok.textContent='✓ Welcome back! Taking you to your dashboard…'; ok.classList.remove('hidden');
    setTimeout(()=>go('dashboard'),1000);
  } catch(ex) { err.textContent=ex.message; err.classList.remove('hidden'); }
  finally { btn.disabled=false; btn.textContent='Sign in'; }
});

document.getElementById('reg-form').addEventListener('submit', async e => {
  e.preventDefault();
  const btn=document.getElementById('reg-btn'), err=document.getElementById('reg-err'), ok=document.getElementById('reg-ok');
  err.classList.add('hidden'); btn.disabled=true; btn.textContent='Creating account…';
  try {
    const r = await api('/auth/register','POST',{ name:document.getElementById('r-name').value.trim(), email:document.getElementById('r-email').value.trim(), password:document.getElementById('r-pw').value });
    const d = await r.json();
    if (!r.ok) throw new Error(d.error||'Registration failed');
    user=d.user; ok.textContent='✓ Account created! Check your email for a welcome message. Redirecting…'; ok.classList.remove('hidden');
    setTimeout(()=>go('dashboard'),1500);
  } catch(ex) { err.textContent=ex.message; err.classList.remove('hidden'); }
  finally { btn.disabled=false; btn.textContent='Create free account'; }
});

async function doForgot() {
  const email=document.getElementById('fp-email').value.trim(), err=document.getElementById('fp-err'), ok=document.getElementById('fp-ok');
  if (!email) { err.textContent='Enter your email.'; err.classList.remove('hidden'); return; }
  await api('/auth/forgot-password','POST',{email});
  ok.textContent='✓ If that email exists, a reset link was sent.'; ok.classList.remove('hidden'); err.classList.add('hidden');
}

async function doReset() {
  const pw=document.getElementById('rp-pw').value, pw2=document.getElementById('rp-pw2').value;
  const err=document.getElementById('rp-err'), ok=document.getElementById('rp-ok');
  if (pw!==pw2) { err.textContent='Passwords do not match.'; err.classList.remove('hidden'); return; }
  const token = new URLSearchParams(location.search).get('token');
  const r = await api('/auth/reset-password','POST',{token,password:pw});
  const d = await r.json();
  if (!r.ok) { err.textContent=d.error; err.classList.remove('hidden'); return; }
  ok.textContent='✓ Password updated! You can now log in.'; ok.classList.remove('hidden');
  setTimeout(()=>go('login'),2000);
}

async function doLogout() {
  try { await api('/auth/logout','POST'); } catch(_) {}
  user=null; go('home');
}

function updateSidebarUser() {
  if (!user) return;
  const n=document.getElementById('sb-name'), e=document.getElementById('sb-email'), p=document.getElementById('sb-plan');
  if (n) n.textContent=user.name;
  if (e) e.textContent=user.email;
  if (p) { p.textContent=user.plan==='pro'?'Pro Plan':user.plan==='starter'?'Starter Plan':'Free Plan'; p.className='uplan'+(user.plan!=='free'?' pro':''); }
}

// ═══════════════════════════════════════
//  DASHBOARD
// ═══════════════════════════════════════
async function initDashboard() {
  try {
    const [sr,dr] = await Promise.all([api('/user/stats'),api('/user/documents')]);
    const stats=await sr.json(), docs=await dr.json();
    document.getElementById('s-total').textContent=stats.total??0;
    document.getElementById('s-plan').textContent=stats.plan==='pro'?'Pro':stats.plan==='starter'?'Starter':'Free';
    document.getElementById('s-rem').textContent=stats.remaining??0;
    const pb=document.getElementById('dash-plan');
    pb.textContent=stats.plan==='pro'?'Pro Plan':stats.plan==='starter'?'Starter Plan':'Free Plan';
    pb.className=`badge ${stats.plan!=='free'?'badge-green':'badge-purple'}`;
    document.getElementById('dash-upgrade').classList.toggle('hidden',stats.plan!=='free'||!user.trialUsed);
    renderDocList(docs.documents||[]);
  } catch(_) { document.getElementById('doc-list').innerHTML='<div class="empty-state"><span class="e-ico">⚠️</span><p>Could not load dashboard. Is the server running?</p></div>'; }
}

function renderDocList(docs) {
  const el=document.getElementById('doc-list');
  if (!docs.length) { el.innerHTML='<div class="empty-state"><span class="e-ico">📄</span><p>No documents yet.</p><button class="btn btn-primary" onclick="go(\'sign\')">Sign your first document</button></div>'; return; }
  el.innerHTML=docs.map(d=>`<div class="doc-row"><div class="doc-icon">📄</div><div class="doc-info"><div class="doc-name">${esc(d.originalName||d.filename)}</div><div class="doc-meta">${fmtDate(d.createdAt)}</div></div><span class="badge badge-green">signed</span></div>`).join('');
}

// ═══════════════════════════════════════
//  BILLING
// ═══════════════════════════════════════
async function initBilling() {
  const body=document.getElementById('billing-body');
  try {
    const r=await api('/billing/subscription'), d=await r.json();
    const sub=d.subscription, paid=sub.plan!=='free';
    body.innerHTML=`
      <div style="max-width:700px;display:flex;flex-direction:column;gap:20px">
        <div class="card">
          <h2 style="font-family:'Cormorant Garamond',serif;font-size:1.3rem;margin-bottom:18px;color:#f0f4ff">Current Subscription</h2>
          <div style="display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid rgba(255,255,255,.06);font-size:.9rem"><span style="color:rgba(226,232,248,.4)">Plan</span><span style="font-weight:600">${paid?(sub.plan==='pro'?'Pro – $4.99/month':'Starter – $1.99/month'):'Free'}</span></div>
          <div style="display:flex;justify-content:space-between;align-items:center;padding:10px 0;font-size:.9rem"><span style="color:rgba(226,232,248,.4)">Status</span><span class="badge ${paid?'badge-green':'badge-purple'}">${paid?'Active':'Free tier'}</span></div>
          ${paid?`<div style="margin-top:18px"><button class="btn btn-danger btn-sm" onclick="cancelSub()">Cancel subscription</button></div>`:''}
        </div>
        ${!paid?`
        <div class="card">
          <h2 style="font-family:'Cormorant Garamond',serif;font-size:1.3rem;margin-bottom:18px;color:#f0f4ff">Upgrade Plan</h2>
          <div style="display:flex;flex-direction:column;gap:12px">
            <div style="border:1.5px solid rgba(255,255,255,.1);border-radius:12px;padding:18px 22px;display:flex;justify-content:space-between;align-items:center;cursor:pointer;transition:all .2s" onclick="go('checkout','starter')" onmouseover="this.style.borderColor='#635bff'" onmouseout="this.style.borderColor='rgba(255,255,255,.1)'">
              <div><div style="font-weight:700;color:#f0f4ff">Starter</div><div style="font-size:.82rem;color:rgba(226,232,248,.4);margin-top:3px">10 documents/month</div></div>
              <div style="font-family:'Cormorant Garamond',serif;font-size:1.6rem;color:#a78bfa;font-weight:700">$1.99/mo</div>
            </div>
            <div style="border:1.5px solid rgba(99,91,255,.5);border-radius:12px;padding:18px 22px;display:flex;justify-content:space-between;align-items:center;cursor:pointer;background:rgba(99,91,255,.08);transition:all .2s" onclick="go('checkout','pro')">
              <div><div style="font-weight:700;color:#f0f4ff">Pro ⚡</div><div style="font-size:.82rem;color:rgba(226,232,248,.4);margin-top:3px">Unlimited documents</div></div>
              <div style="font-family:'Cormorant Garamond',serif;font-size:1.6rem;color:#06d6a0;font-weight:700">$4.99/mo</div>
            </div>
          </div>
        </div>`:''}
      </div>`;
  } catch(_) { body.innerHTML='<div class="empty-state"><p>Could not load billing info.</p></div>'; }
}

async function cancelSub() {
  if (!confirm('Cancel your subscription? You keep access until the end of your current billing period.')) return;
  await api('/billing/cancel','POST'); initBilling();
}

// ═══════════════════════════════════════
//  CHECKOUT
// ═══════════════════════════════════════
function initCheckout(plan='pro') {
  checkoutPlan=plan||'pro';
  const isStarter=checkoutPlan==='starter';
  document.getElementById('co-plan-badge').textContent=(isStarter?'Starter':'Pro')+' Plan';
  document.getElementById('co-price').textContent=isStarter?'$1.99':'$4.99';
  document.getElementById('co-btn').textContent=`Start 7-day free trial →`;
  if (isStarter) {
    document.getElementById('co-feats').innerHTML='<div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> 10 documents/month</div><div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> AI signature placement</div><div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> Camera & draw signature</div><div style="display:flex;align-items:center;gap:10px;font-size:.9rem;color:rgba(226,232,248,.7)"><span style="color:#06d6a0">✓</span> Document history</div>';
  }
}

document.getElementById('co-form').addEventListener('submit', async e => {
  e.preventDefault();
  if (!user) { go('register'); return; }
  const btn=document.getElementById('co-btn'), err=document.getElementById('co-err'), ok=document.getElementById('co-ok');
  err.classList.add('hidden'); btn.disabled=true;
  const price=checkoutPlan==='starter'?'$1.99':'$4.99';
  btn.textContent='Processing…';
  try {
    const r=await api('/billing/create-checkout','POST',{plan:checkoutPlan});
    const d=await r.json();
    if (d.demoMode) {
      // DEMO MODE: simulate payment (real Stripe redirects to Stripe hosted page)
      const r2=await api('/billing/demo-upgrade','POST',{plan:checkoutPlan});
      const d2=await r2.json();
      user={...user,plan:d2.plan};
      ok.textContent=`✓ ${checkoutPlan==='pro'?'Pro':'Starter'} plan activated! 🎉`;
      ok.classList.remove('hidden');
      setTimeout(()=>go('payment-success'),1200);
    } else if (d.url) {
      window.location.href=d.url; // Stripe hosted checkout
    } else throw new Error(d.error||'Checkout failed');
  } catch(ex) { err.textContent=ex.message||'Payment failed. Please try again.'; err.classList.remove('hidden'); }
  finally { btn.disabled=false; btn.textContent=`Start 7-day free trial →`; }
});

function expressPay(m) {
  if (!user) { go('register'); return; }
  alert(`${m==='apple'?'Apple':'Google'} Pay will work once you add your Stripe keys to the .env file. For now, please use the card form below — it works in demo mode!`);
}

function initPaySuccess() {
  const plan=user?.plan||'pro';
  document.getElementById('ps-plan-text').textContent=plan==='pro'?'unlimited':'10 per month';
}

// ═══════════════════════════════════════
//  PROFILE
// ═══════════════════════════════════════
function initProfile() {
  if (!user) return;
  document.getElementById('p-name').value=user.name||'';
  document.getElementById('p-email').value=user.email||'';
  api('/user/signature').then(r=>r.json()).then(d=>{
    if (d.signature) { const img=document.getElementById('saved-sig-img'); img.src=d.signature; img.style.display='block'; document.getElementById('no-sig-txt').style.display='none'; }
  }).catch(()=>{});
}

document.getElementById('prof-form').addEventListener('submit', async e => {
  e.preventDefault();
  const msg=document.getElementById('prof-msg');
  const body={name:document.getElementById('p-name').value.trim()};
  const pw=document.getElementById('p-pw').value;
  if (pw) body.password=pw;
  try {
    const r=await api('/user/profile','PUT',body), d=await r.json();
    if (!r.ok) throw new Error(d.error);
    user={...user,...d.user};
    msg.className='ok-msg'; msg.textContent='✓ Changes saved!'; msg.classList.remove('hidden');
    setTimeout(()=>msg.classList.add('hidden'),3000);
  } catch(ex) { msg.className='err-msg'; msg.textContent=ex.message; msg.classList.remove('hidden'); }
});

// ═══════════════════════════════════════
//  SIGN DOCUMENT
// ═══════════════════════════════════════
function initSign() {
  sigDataURL=null; pdfDoc=null; curPage=1;
  if (document.getElementById('sig-overlay')) document.getElementById('sig-overlay').classList.add('hidden');
  if (document.getElementById('dl-btn')) document.getElementById('dl-btn').disabled=true;
  document.getElementById('sig-ok').classList.add('hidden');
  document.getElementById('pdf-status').classList.add('hidden');

  // Check trial
  if (user?.plan==='free' && user?.trialUsed) {
    document.getElementById('trial-wall').classList.remove('hidden');
    document.getElementById('sign-form').classList.add('hidden');
  } else {
    document.getElementById('trial-wall').classList.add('hidden');
    document.getElementById('sign-form').classList.remove('hidden');
    initCanvas();
  }
}

// PDF upload
document.getElementById('pdf-inp').addEventListener('change', e => { if(e.target.files[0]) loadPDF(e.target.files[0]); });
const dz=document.getElementById('drop-z');
dz.addEventListener('dragover', e=>{e.preventDefault();dz.classList.add('over')});
dz.addEventListener('dragleave', ()=>dz.classList.remove('over'));
dz.addEventListener('drop', e=>{e.preventDefault();dz.classList.remove('over');const f=e.dataTransfer.files[0];if(f)loadPDF(f);});

let currentFileName = 'document.pdf';

async function loadPDF(file) {
  if (file.size>20*1024*1024){showPdfSt('err','File too large. Max 20MB.');return;}
  currentFileName = file.name;
  showPdfSt('loading','⏳ Loading '+file.name+'…');
  const buf=await file.arrayBuffer();
  originalPdfBytes = new Uint8Array(buf); // save for pdf-lib
  try {
    pdfDoc=await pdfjsLib.getDocument({data:buf}).promise;
    curPage=1; await renderPDF(curPage); updatePgLbl();
    showPdfSt('ok','✓ Loaded: '+file.name+' ('+pdfDoc.numPages+' page'+(pdfDoc.numPages>1?'s':'')+')');
    if(sigDataURL) enableDL();
  } catch(_){showPdfSt('err','Could not read this PDF. Make sure it\'s a valid file.');}
}

function showPdfSt(t,msg){
  const el=document.getElementById('pdf-status');
  el.className=t==='ok'?'ok-msg':t==='err'?'err-msg':'warn-msg';
  el.textContent=msg; el.classList.remove('hidden');
}

async function renderPDF(n) {
  const page=await pdfDoc.getPage(n);
  const wrap=document.getElementById('pdf-canvas').parentElement;
  const scale=Math.min((wrap.clientWidth||600)/page.getViewport({scale:1}).width,2);
  const vp=page.getViewport({scale});
  const canvas=document.getElementById('pdf-canvas');
  canvas.width=vp.width; canvas.height=vp.height;
  await page.render({canvasContext:canvas.getContext('2d'),viewport:vp}).promise;
}

function updatePgLbl(){if(!pdfDoc)return;document.getElementById('pg-lbl').textContent=`Page ${curPage}/${pdfDoc.numPages}`;document.getElementById('prev-btn').disabled=curPage<=1;document.getElementById('next-btn').disabled=curPage>=pdfDoc.numPages;}
async function changePg(d){const n=curPage+d;if(n<1||n>pdfDoc?.numPages)return;curPage=n;await renderPDF(n);updatePgLbl();}

// Drawing
function initCanvas() {
  const c=document.getElementById('sig-canvas'), ctx=c.getContext('2d');
  ctx.clearRect(0,0,c.width,c.height);
  const pos=e=>{const r=c.getBoundingClientRect(),sw=c.width/r.width,sh=c.height/r.height,s=e.touches?e.touches[0]:e;return[(s.clientX-r.left)*sw,(s.clientY-r.top)*sh]};
  c.onmousedown=e=>{isDrawing=true;[lx,ly]=pos(e)};
  c.onmousemove=e=>{if(!isDrawing)return;const[x,y]=pos(e);ctx.beginPath();ctx.moveTo(lx,ly);ctx.lineTo(x,y);ctx.strokeStyle=drawClr;ctx.lineWidth=2.5;ctx.lineCap='round';ctx.stroke();[lx,ly]=[x,y]};
  c.onmouseup=c.onmouseleave=()=>isDrawing=false;
  c.ontouchstart=e=>{e.preventDefault();isDrawing=true;[lx,ly]=pos(e)};
  c.ontouchmove=e=>{e.preventDefault();if(!isDrawing)return;const[x,y]=pos(e);ctx.beginPath();ctx.moveTo(lx,ly);ctx.lineTo(x,y);ctx.strokeStyle=drawClr;ctx.lineWidth=2.5;ctx.lineCap='round';ctx.stroke();[lx,ly]=[x,y]};
  c.ontouchend=()=>isDrawing=false;
}

function setClr(el){document.querySelectorAll('.swatch').forEach(s=>s.classList.remove('active'));el.classList.add('active');drawClr=el.dataset.c;}
function clearSig(){const c=document.getElementById('sig-canvas');c.getContext('2d').clearRect(0,0,c.width,c.height);sigDataURL=null;document.getElementById('sig-ok').classList.add('hidden');}
function useDraw(){
  sigDataURL=document.getElementById('sig-canvas').toDataURL();
  document.getElementById('sig-ok').classList.remove('hidden');
  saveSigToProfile();
  if(pdfDoc){
    const ins=document.getElementById('ai-instruction').value.trim();
    if(ins) doAIPlace(); else placeSig();
  } else {
    showSignatureReadyPrompt();
  }
}

function showSignatureReadyPrompt(){
  document.getElementById('sig-ok').textContent='✅ Signature ready — now upload your PDF in Step 1, then use AI Place in Step 3';
  document.getElementById('sig-ok').classList.remove('hidden');
}

// Camera
async function openCam(){
  try{camStream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});const v=document.getElementById('cam-vid');v.srcObject=camStream;document.getElementById('cam-open-btn').classList.add('hidden');document.getElementById('cam-cap-btn').classList.remove('hidden');}
  catch(_){alert('Camera access denied. Please allow camera in your browser settings.');}
}
function captureCam(){const v=document.getElementById('cam-vid'),c=document.getElementById('cam-cvs');c.width=v.videoWidth;c.height=v.videoHeight;c.getContext('2d').drawImage(v,0,0);stopCam();const p=document.getElementById('cam-prev');p.src=c.toDataURL();p.style.display='block';document.getElementById('cam-cap-btn').classList.add('hidden');document.getElementById('cam-ret-btn').classList.remove('hidden');document.getElementById('cam-use-btn').classList.remove('hidden');}
function retakeCam(){document.getElementById('cam-prev').style.display='none';document.getElementById('cam-ret-btn').classList.add('hidden');document.getElementById('cam-use-btn').classList.add('hidden');document.getElementById('cam-open-btn').classList.remove('hidden');}
function useCam(){
  sigDataURL=document.getElementById('cam-cvs').toDataURL();
  document.getElementById('sig-ok').classList.remove('hidden');
  saveSigToProfile();
  if(pdfDoc){ const ins=document.getElementById('ai-instruction').value.trim(); if(ins) doAIPlace(); else placeSig(); }
  else showSignatureReadyPrompt();
}
function stopCam(){if(camStream){camStream.getTracks().forEach(t=>t.stop());camStream=null;}const v=document.getElementById('cam-vid');if(v)v.srcObject=null;}

function loadSigFile(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=ev=>{sigDataURL=ev.target.result;const p=document.getElementById('sig-file-prev');p.src=sigDataURL;p.style.display='block';document.getElementById('sig-ok').classList.remove('hidden');if(pdfDoc)placeSig();saveSigToProfile();};r.readAsDataURL(f);}

function switchTab(name,btn){document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.tab-panel').forEach(p=>p.classList.add('hidden'));document.getElementById('tab-'+name).classList.remove('hidden');if(name!=='camera')stopCam();}

async function saveSigToProfile(){try{await api('/user/signature','POST',{dataURL:sigDataURL});}catch(_){}}

// AI Placement
function setAI(text){
  document.getElementById('ai-instruction').value=text;
  // Auto-trigger if we already have both doc and signature
  if(sigDataURL && pdfDoc) doAIPlace();
}

async function doAIPlace(){
  if(!sigDataURL){ alert('Please capture or draw your signature first in Step 2.'); return; }
  if(!pdfDoc){ alert('Please upload your PDF document first in Step 1.'); return; }
  const instruction = document.getElementById('ai-instruction').value.trim() || 'bottom right';
  const canvas = document.getElementById('pdf-canvas');
  document.getElementById('ai-thinking').classList.remove('hidden');
  document.getElementById('ai-result').classList.add('hidden');
  try {
    const r = await api('/user/ai-place','POST',{ instruction, pageWidth:canvas.width, pageHeight:canvas.height });
    const d = await r.json();
    setTimeout(()=>{
      document.getElementById('ai-thinking').classList.add('hidden');
      placeSig(d.x, d.y, d.width, d.height);
      const res = document.getElementById('ai-result');
      document.getElementById('ai-result-txt').textContent = `Signature placed — "${instruction}"`;
      res.classList.remove('hidden');
    }, 900);
  } catch(_) {
    document.getElementById('ai-thinking').classList.add('hidden');
    placeSig(); // fallback default
  }
}

function placeSig(x,y,w,h){
  if(!sigDataURL||!pdfDoc)return;
  const overlay=document.getElementById('sig-overlay'), img=document.getElementById('sig-ov-img'), wrap=document.querySelector('.pdf-viewer-wrap');
  img.src=sigDataURL;
  const canvas=document.getElementById('pdf-canvas'), cr=canvas.getBoundingClientRect(), wr=wrap.getBoundingClientRect();
  const scaleX=cr.width/canvas.width, scaleY=cr.height/canvas.height;
  overlay.style.left=((x||canvas.width*0.6)*scaleX)+'px';
  overlay.style.top=((y||canvas.height*0.82)*scaleY)+'px';
  overlay.style.width=((w||200)*scaleX)+'px';
  overlay.classList.remove('hidden');
  enableDL();

  // Drag
  let drag=false,ox=0,oy=0;
  overlay.onmousedown=e=>{if(e.target.id==='rz-handle')return;drag=true;const or=overlay.getBoundingClientRect(),wwr=wrap.getBoundingClientRect();ox=e.clientX-(or.left-wwr.left);oy=e.clientY-(or.top-wwr.top);e.preventDefault()};
  document.onmousemove=e=>{if(!drag)return;const wwr=wrap.getBoundingClientRect();overlay.style.left=Math.max(0,e.clientX-wwr.left-ox)+'px';overlay.style.top=Math.max(0,e.clientY-wwr.top-oy)+'px'};
  document.onmouseup=()=>drag=false;

  // Resize
  let rz=false,sw=0,sx2=0;
  document.getElementById('rz-handle').onmousedown=e=>{rz=true;sw=overlay.offsetWidth;sx2=e.clientX;e.preventDefault();e.stopPropagation()};
  document.addEventListener('mousemove',e=>{if(!rz)return;overlay.style.width=Math.max(60,sw+(e.clientX-sx2))+'px'});
  document.addEventListener('mouseup',()=>rz=false);
}

function enableDL(){if(pdfDoc&&sigDataURL)document.getElementById('dl-btn').disabled=false;}

// Store original PDF bytes for proper embedding
let originalPdfBytes = null;

async function signAndDownload(){
  if(!pdfDoc){ alert('Please upload a PDF first in Step 1.'); return; }
  if(!sigDataURL){ alert('Please add your signature first in Step 2.'); return; }

  const btn = document.getElementById('dl-btn');
  const lbl = document.getElementById('sign-lbl');
  btn.disabled = true;
  lbl.textContent = '⏳ Generating signed PDF…';
  lbl.className = 'status-lbl';

  try {
    const pCanvas = document.getElementById('pdf-canvas');
    const overlay = document.getElementById('sig-overlay');

    // If signature not placed yet — auto place bottom right
    if(overlay.classList.contains('hidden')){
      placeSig(
        Math.round(pCanvas.width  * 0.62),
        Math.round(pCanvas.height * 0.84),
        Math.round(pCanvas.width  * 0.28),
        Math.round(pCanvas.height * 0.07)
      );
      await new Promise(r => setTimeout(r, 300));
    }

    const cr = pCanvas.getBoundingClientRect();
    const or = overlay.getBoundingClientRect();

    // Scale from display pixels → canvas pixels
    const scaleX = pCanvas.width  / cr.width;
    const scaleY = pCanvas.height / cr.height;

    const sx = (or.left - cr.left) * scaleX;
    const sy = (or.top  - cr.top)  * scaleY;
    const sw = or.width  * scaleX;
    const sh = or.height * scaleY;

    // ── Method 1: Real PDF embedding ──────────────────────
    if(originalPdfBytes && window.PDFLib){
      try{
        const { PDFDocument } = PDFLib;
        const pdfDoc2 = await PDFDocument.load(originalPdfBytes, { ignoreEncryption:true });
        const pages   = pdfDoc2.getPages();
        const page    = pages[Math.min(curPage-1, pages.length-1)];
        const { width:pW, height:pH } = page.getSize();

        const rx = pW / pCanvas.width;
        const ry = pH / pCanvas.height;

        const pdfX =  sx * rx;
        const pdfY =  pH - (sy * ry) - (sh * ry);
        const pdfW =  sw * rx;
        const pdfH =  sh * ry;

        const base64 = sigDataURL.split(',')[1];
        const bytes  = Uint8Array.from(atob(base64), c => c.charCodeAt(0));
        const isPng  = sigDataURL.startsWith('data:image/png');
        const img    = isPng ? await pdfDoc2.embedPng(bytes) : await pdfDoc2.embedJpg(bytes);

        page.drawImage(img, { x:pdfX, y:pdfY, width:pdfW, height:pdfH });

        const out  = await pdfDoc2.save();
        const blob = new Blob([out], { type:'application/pdf' });
        triggerDownload(blob, (currentFileName||'document').replace(/\.[^.]+$/,'')+'_signed.pdf');
        lbl.textContent = '✅ Signed PDF downloaded!';
        lbl.className = 'status-lbl ok';
        logDocument();
        btn.disabled = false;
        return;
      } catch(e){ console.warn('pdf-lib error, using canvas fallback:', e); }
    }

    // ── Method 2: Canvas flatten — ALWAYS works ───────────
    const out = document.createElement('canvas');
    out.width  = pCanvas.width;
    out.height = pCanvas.height;
    const ctx  = out.getContext('2d');
    ctx.drawImage(pCanvas, 0, 0);

    const sigImg = new Image();
    sigImg.crossOrigin = 'anonymous';
    await new Promise((res, rej) => { sigImg.onload=res; sigImg.onerror=rej; sigImg.src=sigDataURL; });
    ctx.drawImage(sigImg, sx, sy, sw, sh);

    out.toBlob(blob => {
      if(!blob){ lbl.textContent='❌ Could not create file. Try a different browser.'; lbl.className='status-lbl err'; btn.disabled=false; return; }
      triggerDownload(blob, (currentFileName||'document').replace(/\.[^.]+$/,'')+'_signed.png');
      lbl.textContent = '✅ Signed document downloaded!';
      lbl.className = 'status-lbl ok';
      logDocument();
      btn.disabled = false;
    }, 'image/png', 0.95);

  } catch(ex){
    console.error('Download error:', ex);
    lbl.textContent = '❌ ' + (ex.message || 'Something went wrong — try again');
    lbl.className = 'status-lbl err';
    btn.disabled = false;
  }
}

function triggerDownload(blob, filename) {
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  setTimeout(() => { URL.revokeObjectURL(a.href); a.remove(); }, 1000);
}

function logDocument() {
  api('/user/documents', 'POST', {
    filename: (currentFileName || 'signed-document.pdf'),
    originalName: (currentFileName || 'signed-document.pdf')
  }).then(() => { if(user) user.trialUsed = true; }).catch(() => {});
}

// ═══════════════════════════════════════
//  CONTACT
// ═══════════════════════════════════════
function sendContact(){const ok=document.getElementById('contact-ok');ok.classList.remove('hidden');setTimeout(()=>ok.classList.add('hidden'),5000);}

// ═══════════════════════════════════════
//  UTILITIES
// ═══════════════════════════════════════
function esc(s){return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}
function fmtDate(iso){return iso?new Date(iso).toLocaleDateString('en-US',{year:'numeric',month:'short',day:'numeric'}):'–'}

// ═══════════════════════════════════════
//  INIT
// ═══════════════════════════════════════
(async()=>{
  document.getElementById('yr').textContent=new Date().getFullYear();
  document.querySelectorAll('.yr2').forEach(el=>el.textContent=new Date().getFullYear());
  await checkSession();
  // Detect current page from URL
  const path=location.pathname.replace('/','').split('?')[0]||'home';
  const plan=new URLSearchParams(location.search).get('plan');
  const valid=Object.keys(PAGE_MAP);
  go(valid.includes(path)?path:'home', plan);
  // Handle verify token
  const vt=new URLSearchParams(location.search).get('verified');
  if(vt==='1'&&document.getElementById('pg-dashboard').classList.contains('active')){const b=document.createElement('div');b.className='ok-msg';b.textContent='✓ Email verified! Welcome to SignForge.';b.style='position:fixed;top:80px;right:24px;z-index:999;max-width:340px';document.body.appendChild(b);setTimeout(()=>b.remove(),5000);}
})();
</script>
</body>
</html>








